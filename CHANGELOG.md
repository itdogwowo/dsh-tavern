# 變更記錄

版本號照 [Semantic Versioning](https://semver.org/lang/zh-TW/)。

## 2.6.42 — 附件（上傳檔案與圖片）

使用者回報「沒法選擇模型和上傳檔案」。模型那一半 2.6.22–2.6.41 已經做完，**這一版補上
附件**：對話頁輸入框左邊多一顆 📎，可以夾帶圖片與檔案。

**兩條路，照 DSH 自己的 composer 分**（讀 `dsh-client-ui-conversation` 的 `sendSession`
得到的，不是猜的）：

- **圖片** → `{type:'image', mediaType, data(base64), name}` 直接跟著 prompt 送，
  宿主把它變成耐用附件。**只有這樣模型才真的看得到那張圖。**
- **其他檔案** → 先 `ctx.get('fileUpload').upload(sessionId, file, name, …)` 拿
  `receiptId`，prompt 裡放 `{type:'file', receiptId}`（那是一次性憑證，綁在那個 session 上）。
- 附件在**前面**、文字在後面（DSH 的順序）；只有附件沒有文字是合法的。
- 三件事都在真瀏覽器上量過：檔案附件——模型回覆裡寫著
  「The user attached a file "筆記.txt" (12 bytes)」；圖片附件——模型看得出
  「primarily red/crimson with a yellow/gold block in the upper-left corner」。

**房間裡再存一份普通檔案**（`chats/<角色>/<房間id>/files/`）：DSH 那一份住在它的內部
儲存裡（使用者看不到、帶不走），而酒館的立場是「這個資料夾帶走就好」。訊息用
`extra.media` 指向房間那一份（**SillyTavern 的欄位**），所以重新整理之後還畫得出來。

- 新的宿主半 op：`file.write`（二進位 body，跟插圖上傳同一條路）／`file.list`／`file.delete`，
  加上第三條 HTTP 路由 `/api/dsh-tavern/files`（跟插圖同一組來源圍籬與 etag）。
- 附件資料夾**不跟插圖共用 `art/`**：插圖是「這個東西長什麼樣」（海報牆、主圖、表情圖挑選器
  都在讀它），附件是「這一則訊息夾帶了什麼」。混在一起會讓主圖挑到一個 PDF。
- 讀取路由：非圖片一律 `content-disposition: attachment` ＋ `nosniff`——丟進來的
  HTML／SVG 不可以被當成文件執行。
- `chat.jsonl` 的訊息多一個 `extra.media`（`[{type,url,name,bytes}]`）；**沒有附件就不寫**
  那個欄位（既有檔案的形狀不變）。空正文＋有附件**寫得進去**（丟一張圖不說話是合法的）
  ——以前空正文整則被丟掉。

**同場修掉兩個真的壞掉的地方**（都是這一輪在真瀏覽器上量到的）：

1. ⚠️ **按下送出的同一瞬間，自己那則訊息（連同附件）會從畫面消失。**
   `setChatRunning()` 會 bump `refreshChannel`（側邊欄那一列的跑馬燈要重畫），而對話頁
   訂閱了它並在裡面 `loadMessages()`——磁碟上還沒有剛送出的那一則（要等跑完才寫回），
   所以重讀等於把它蓋掉。而且那一輪失敗時的錯誤訊息會被 `loadMessages` 的成功分支
   （`chat.error = ''`）一起蓋掉，症狀變成「按了送出，什麼都沒發生」。
   修法：這一輪還在跑（`chat.busy`）或還在寫回紀錄（`chat.writing`）時不要重讀。
2. **附件 chip 在送出時整顆不見**：`media` 原本只收「已經有 URL」的，而 URL 要等存進
   房間才有（送出之後的事）。改成**名字在就留著**——還沒存到時畫一顆**不能點**的 chip
   （虛線框），比什麼都不畫誠實。

> ⚠️ **宿主半改了，要重啟 `dsh web`** 才會生效。在那之前：附件**送得出去、模型也看得到**，
> 但房間那一份存不下來（`file.write` 還是 `unknown op`），所以重新整理之後訊息上不會有附件。
> 瀏覽器半（選單、chip、訊息上的圖）重新載入頁面就生效。

## 2.6.41 — 撤回「進房就讀目錄」（畫面會整個不見）

2.6.40 加了「換房間時順便讀模型目錄」，想讓 chip 一進房就有模型名。結果在瀏覽器上量到：
**整個 app 都不再渲染**——`[data-slot="main"]` 是 0、連側邊欄的房間列都不見，
而前一版一模一樣的操作還好好的。嫌疑最大的就是那一行（它在 render 的同一輪裡觸發
另一個非同步流程＋`render()`）。

已撤回：目錄**回到「點開選單才讀」**（原本已驗過的行為）。代價是 chip 要點一次才會從
「模型」變成模型名——**這個代價比整個畫面不見小得多**。

⚠️ 這裡也記一條規矩：**進房那條 effect 裡不要再塞新的非同步讀取**。它已經有
`loadMessages()`／`loadUsage()` 兩個，加第三個就出事——要加就先單獨驗那一項。
## 2.6.40 — 進房就讀模型目錄（chip 立刻有名字）

使用者：「進入不會先顯示」。2.6.39 已經會用目錄的 `default` 補「目前是哪個模型」，
但**目錄是點開選單才讀的**——所以一進房 chip 還是「模型」，要點一次才會變。
現在換房間時就跟訊息、用量一起讀（`loadModels()` 有快取，只讀一次）。

DSH 的 chip 一進畫面就有名字，靠的就是同一份目錄——差別只在它一開始就讀。
## 2.6.39 — 兩件「餘下的」收尾

### ① 靠右對齊為什麼沒生效（同權重靠順序贏）

`left:auto;right:8px` 寫在我的 `.dsh-tv-modelPanel`，而 `.dsh-tv-usagePanel`（`left:50%`）
在 CSS 陣列裡**排在後面**——同權重時後面的贏，所以我那兩行被蓋掉，面板因此偏掉
（量到 `rightEdgeNearChip: false`）。改成 `.dsh-tv-usagePanel.dsh-tv-modelPanel`（兩個 class，
權重比較高）**不靠順序**——順序是會隨檔案編輯改變的東西，靠它會再壞一次。

### ② 一進房就有模型名：用目錄自己的 default

使用者說「進入不會先顯示」。先前只有兩個來源（自己換過的、訊息裡的 `extra.route`），
兩個都要「有動作」才有值。DSH 的答案在目錄裡：**`ModelCatalog.default`**（未設定的 session
用它）。所以選單一讀到目錄、而手上還沒有任何選擇時，就用 `default` 填上——chip 立刻有模型名，
選單裡那一列也才會展開推理等級（`effortRows` 先前是 0 就是因為「目前是哪個」是空的）。

### 還沒做：共用 `Button`（第三次說明，這次講清楚為什麼不是「不想做」）

`Button` 有自己的高度／內距／`variant` 樣式。我這邊已經**逐項量過**並對齊好的位置是：
送出鍵 34×34 圓形、模型 chip 28px、上下文環 28×28、pill 兩顆 13px。
換成 `Button` 會在**同一次改動裡同時動到四組數字**，而驗證方式是「一眼看不出差別」的目視比對
——那正是這一輪已經出過三次錯的地方（改完沒當場量）。要做的話應該是**一次只換一顆、
換完就在頁面上量那一顆的尺寸**，而不是一次全換。這是我建議的下一輪第一步。
## 2.6.38 — 等級跟著路由一起存（重啟後生效）＋ 選單靠右對齊 chip

使用者：「請繼續」。

### ① 等級（effort）也存進訊息裡

2.6.20 只存了 `extra.route = { provider, model }`，所以**一進房看得到模型名、看不到等級**
（等級只有「自己剛換過」那一次才有）。現在 `extra.route` 多一個 `effort`：
`appendRoomMessages` 寫、`readRoomMessages` 讀，進房時一起還原到 chip 上
（DSH 的 `_7KE1Ra_triggerEffort`，例如 `Max`）。

⚠️ **這一項動到宿主半 → 要重啟 `dsh web`**（跟 2.6.19 的 `extra.usage`／`extra.ms` 同一批）。
重啟之後**新的回合**才會把 effort 寫進檔案；在那之前的訊息沒有那格。

### ② 選單改成靠右對齊（不再 portal）

chip 就在卡片的右緣，所以面板**靠右對齊**就會視覺上貼著它——不必 portal、不必算座標，
也就不會再出現「座標算錯 → 看不見」（那是我前兩輪讓使用者看不到的原因）。
`left:auto;right:8px;transform:none` 蓋掉 `.dsh-tv-usagePanel` 的置中那兩行。

**看得見優先**這條規矩現在寫進 CSS 的註解裡：任何「錨定」的巧思，只要可能讓東西消失，
就不採用——除非有餘裕當場在頁面上驗過。

### 還沒做：`Button`

`@deepseek-ai/dsh-client-ui-primitives` 的 `Button` **有它自己的高度與內距**，換上去會讓我
已經實測對齊的位置（送出鍵 34×34 圓形、chip 28px、環 28×28）全部位移——那需要一次完整的
目視比對。這一輪仍然**刻意不動**：與其把已經對齊好的東西弄歪，不如等有餘裕時一次換完再驗。
## 2.6.37 — 一進房就顯示目前用哪個模型（DSH 的做法）

使用者：「現在進入不會先顯示，而是我選擇了後才顯示」——這是對的，因為 chip 的標籤只有兩個來源：
我自己換過的（記憶體，重新整理就沒了）、或用量列讀到的 `request/context`（要等下一次請求）。
所以剛進房時它顯示「模型」。

DSH 不是這樣：它的 `ModelDirectory` **一進 session 就從 `modelSelection` 投影**讀目前選什麼
（`binding.session.projections.faceOf('modelSelection')` → `{lastUsed, next}`）。

這裡用**不需要新 API** 的方式做到同一件事：酒館自己把每一輪的路由記在訊息的
`extra.route` 裡（2.6.20 就做了），所以**往前掃最後一則有記錄的訊息**就是「目前用什麼」。
已經自己換過（`chat.modelCurrent` 有值）就不覆蓋。

（等級那一格還是只有「自己換過」之後才有——`extra.route` 目前只存 provider／model，
要一起存 effort 得動宿主半，留給下一輪。）
## 2.6.36 — 選單改回「看得見優先」（portal 先關掉）

使用者：「更新進去好像沒有分別完全看不見」「選單也看不見模型的 id」。

我在自己的瀏覽器上量到**確切的數字**：選單內容完全正確——每一列兩行，
`DeepSeek-V41-Flash` ／ `deepseek-official/deepseek-flash`（**id 出來了**，使用者要的）；
portal 也成功（`parentIsBody: true`、`zIndex: 1100`、四個模型）——
**但座標是 `top: -248, left: 8`**：面板被丟到畫面外，所以「完全看不見」。

portal 之後座標要自己算，而它需要的是一次有餘裕的定位實作（量測 ＋ 邊界碰撞 ＋ 捲動時重算）。
這一輪的 context 已經見底，所以**先把 portal 關掉**，回到跟其他面板同一套的絕對定位
——那一版我在頁面上實測看得見。**看得見優先**：位置差一點可以忍，看不見不能。

### 順便（給使用者的檢查法）

客戶端 bundle 是宿主半從磁碟送的，**改了原始碼瀏覽器仍可能拿快取**。滑到側邊欄「酒館街」
上面，提示會寫 `dsh-tavern tavern-client-2.6.36`；看到舊號碼就是快取，請 **Ctrl+Shift+R**。
## 2.6.35 — 選單顯示路由 id（兩行），以及「請確認你跑的是新版本」

使用者：「你是不是沒有幫我安裝，更新進去好像沒有分別」「選單也看不見模型的 id」
「我預期我是能夠看見 `_7KE1Ra_triggerLabel`（模型名）與 `_7KE1Ra_triggerEffort`（Max）」。

### ① 先確認在跑哪一版（這一條比什麼都重要）

客戶端 bundle 是宿主半從磁碟送的——**改了原始碼，瀏覽器還可能拿快取**。滑到側邊欄
「酒館街」上面，提示會寫 `dsh-tavern tavern-client-2.6.35`；看到舊號碼就 **Ctrl+Shift+R**。
（我這邊量的 build 是新版，所以檔案有送出去；看不到差別通常是快取。）

### ② 選單每一列改成兩行：模型名 ＋ 路由 id

DSH 的選項是兩行（`._7KE1Ra_optionCopy` 是 `flex-direction:column`），第一行
`_7KE1Ra_modelName`（14px／500），第二行是路由本身。照它做：第一行模型名、第二行
`provider/model-id`——使用者要看得見 id。

### ③ chip 的模型名與等級

換過模型之後 chip 會顯示**模型名 ＋ 等級**（`_7KE1Ra_triggerLabel`／`_7KE1Ra_triggerEffort`）。
沒換過之前顯示用量列讀到的路由；兩者都沒有才顯示「模型」——那是因為**還沒問到**
這一間房目前用什麼（要靠下一次請求的 `request/context`）。
## 2.6.34 — 座標不合法就不要 portal（退回看得見的那一版）

使用者：「首先現在選擇方法已經消失了」。

我在自己的瀏覽器上量到的是：`menuOpen: true`、`parentIsBody: true`（portal 成功）、
`zIndex: 1100`、四個模型都在——**但 `visible: false`**，也就是面板被丟到畫面外。

原因：座標只要有任何一個不是有限數（量到 `NaN`），`bottom` 就會寫成 `NaNpx`，
那個 CSS 無效 → 面板回到「自動」位置 → 跑到畫面外。這種「壞資料換來一個看不見的面板」
比原本的畫法更糟。

所以現在**先驗四個數字**（`left`／`top`／`viewport` 是有限數、`viewport > 0`），
全部合法才 portal；否則 `panelStyle` 保持 `undefined`，**退回原本那一版**（絕對定位，
看得見優先——那一版是我在頁面上實測過的）。
## 2.6.33 — portal 之後 z-index 要跟上（不然選單被蓋住）

使用者：「首先現在選擇方法已經消失了」。

我在自己的瀏覽器上量過：chip／環／送出／pill **全部都在**（build 2.6.32、`errors: []`），
所以不見的不是 chip，是**點開之後的選單**。

原因：面板 portal 到 `document.body` 之後，它要跟**整個 app 的圖層**競爭，而 `z-index` 只有
100（那是它還待在卡片裡時夠用的值）——DSH 自己的選單用 **1100**。太低就被別的層蓋住，
看起來就是「選單不見了」。現在 portal 出去的面板帶 `zIndex: 1100`。

教訓（跟這幾輪同一條）：**改動畫面的「所在位置」時，所有跟著位置而來的屬性都要一起想**
——containing block、z-index、座標系，一個沒跟上就是「不見了」。
## 2.6.32 — 選單 portal 到 document.body（這才是錨在 chip 上的正確做法）

使用者：「一次過做曬先讓我驗證」。

`position:fixed` 的 containing block 不是視窗——只要**任何祖先**有 `transform`／`filter`／
`contain` 就會變成那個祖先。這個面板住在對話卡片裡，所以上次整個跑到畫面外。

現在照**這個 repo 已經有先例**的做法（側邊欄 ⋯ 的入口選單 `paintChatMenu`）：用
`ReactDOM.createPortal` 把面板掛到 `document.body`，在那裡 fixed 的基準才是視窗，座標
（量 chip 的 `getBoundingClientRect()`）才算得對——貼在 chip 上方 8px、靠右緣會推回畫面內。

**兩層保險**（因為已經讓使用者看過三次空白／看不見）：
1. 拿不到 `react-dom`／`createPortal` → `CREATE_PORTAL` 是 `null`，選單照原本的畫法畫（看得見）。
2. 量不到 chip（離線測試沒有真的 DOM）→ `panelStyle` 是 `undefined`，同樣退回原本的畫法。

### 沒有做的一件：`Button` 換成共用元件

`@deepseek-ai/dsh-client-ui-primitives` 裡確實有 `Button`（用 `variant`／`disabled`／`onClick`），
但**它有自己的高度與內距**——換上去我驗過的那些位置（送出鍵 34×34 圓形、模型 chip 28px、
環 28×28）都會跟著位移，而那需要一次完整的目視比對。這一輪的 context 已經見底，
所以**刻意不動**：與其換一半、把已經對齊好的東西弄歪，不如留給下一輪有餘裕時做。
## 2.6.31 — 換完要記得「現在用什麼」＋ 推理等級那層做出來了

使用者：「已把這一間房的模型換成 deepseek-official/deepseek-flash」（換成功了 ✓）
＋「現在用什麼他已經沒有顯示了」「思考深度也未能設定」。

### ① 換完沒記下來，所以 chip 還在顯示預設字

`pickModel` 成功只寫了通知，**沒有把選到的東西記下來**——chip 的標籤還是靠用量列讀到的
`request/context`，而那個要等下一次請求才更新，所以換完當下（以及重啟後）它一直顯示「模型」。

現在優先序是：**我自己換過的**（最準，含等級）→ 用量列讀到的路由 → 預設字。

### ② 推理等級（使用者說的「深度選擇」）

選單裡**選中的那個模型底下**會展開它的等級（`ModelCatalogModel.reasoning.efforts`），
最後一列是「提供方預設」；挑了就連 `reasoningEffort` 一起送給 `selectModel`
（那條路本來就吃這個欄位）。chip 上也會跟著顯示等級（DSH 的 `_7KE1Ra_triggerEffort`，例如 `Max`）。

CSS 照 DSH 的層級：等級比模型小一級、縮排（左邊留出圖示寬度）、`min-height:30px`。
## 2.6.30 — 讓錯誤自己說出「它到底有什麼」

使用者：「好像仍然設定不了…仍然是哪一句，麻煩看一看你自己的代碼」。

我自己看了：`selectModel` 在型別裡跟**能用的** `modelCatalog` 相鄰（同一份
`typert.remote-client.d.ts`，第 23 行與第 29 行），所以命名空間的判斷沒錯——問題是
**執行時那個物件上實際有什麼**。這種事猜不出來，只能列出來（跟查 primitives 同一招）。

所以那一句錯誤訊息現在會把 `Object.keys(remote.session)` **整串印出來**：

```
這台 DSH 的 remote.session 沒有 selectModel；它有的是：…（一串方法名）
```

下次貼那一行，就能直接看出正確的方法名（或看出它是 Proxy、要用別的方式取）。
## 2.6.29 — 先把「看不見」修回來（fixed 定位的陷阱）

使用者：「完全看不見」。

我把模型選單改成 `position:fixed` 並用 chip 的 rect 算座標——**它在我的瀏覽器上驗過是好的**
（`panelAboveChip: true`），但在使用者的畫面上整個不見。原因是我漏掉的 CSS 事實：
**`position:fixed` 的基準不是視窗，只要任何祖先有 `transform`／`filter`／`contain`，
基準就變成那個祖先**——面板於是算到畫面外。

現在回到原本的絕對定位（跟其他面板同一套，**看得見優先**）。要真的錨在 chip 上的正確做法是
**portal 到 `document.body`**——這個 repo 已經有先例：側邊欄那顆 ⋯ 的選單就是 portal
（`paintChatMenu`），照它做就不會踩到 containing block 的陷阱。下一輪照這個做。
## 2.6.28 — 選單錨在 chip 上（真的通了）

使用者：「浮動的方格也不是在選單上面…請你認真查看這部分的代碼」。

DSH 的 `._7KE1Ra_menu` 是 **`position:fixed` ＋ 用觸發鈕的 `getBoundingClientRect()` 算座標**，
我卻把面板 `position:absolute` 掛在 pill 那一排的容器上——所以位置一定偏。現在：

- 點 chip 時量它的 rect（`measureChip`，量不到就回 `null`，那時退回原本的絕對定位，**不會消失**）；
- 面板 `position:fixed`：`left` 對齊 chip 的左緣、`bottom` 讓它貼在 chip **上方**，並在靠右緣時推回畫面內。

**真的頁面上驗過**：`menuOpen: true`、`panelAboveChip: true`、分組標題「DeepSeek」、
四個真實模型名（`DeepSeek-V41-Flash`／`DeepSeek-V4-Flash`／`DeepSeek-V4-Pro`／`DeepSeek-V4-Flash-Vision-Exp`）。
（先前那個 undefined 的 bug 修掉之後就沒有再空白了。）

### 還沒做的：推理等級（「沒有深度選擇」）

`ModelCatalogModel.reasoning = { efforts: [{id, name, description?}], defaultEffort? }` 已經查到，
`pickModel` 也早就吃 `reasoningEffort`；還差的是把那一層畫出來（選中的模型底下展開等級，
外加「提供方預設」），以及 chip 上的 `_7KE1Ra_triggerEffort`（例如 `Max`）——
chip 那邊我已經留好 `chat.modelEffort` 的顯示位置，接上去就有。
## 2.6.27 — selectModel 掛錯命名空間＋選單照 DSH 的真實度量重做

使用者回報兩件：「這台 DSH 沒有提供模型切換（sessions.selectModel）」＋「你的方格設計得不夠 dsh」。

### ① 命名空間掛錯了

`selectModel` 跟**能用的** `modelCatalog` 是**同一個命名空間**（`remote.session`），我卻把它掛在
`ctx.get('sessions')` 上——所以永遠是 `undefined`，訊息就是使用者看到的那一句。修成同一個服務。

（證據不是我猜的：能跑的那個呼叫就是 `remote.session.modelCatalog()`，兩個方法在同一層。）

### ② 選單重做（讀 `._7KE1Ra_*` 的真實 CSS，不是憑感覺）

| | 我先前 | DSH 真實的 | 現在 |
|---|---|---|---|
| 選單本體 | `padding:10px 12px`、圓角 10 | `padding:4px`、圓角 20px、`max-height:min(360px,100vh-96px)` | 照它 |
| 分組標題 | 11px、靜態 | **`position:sticky;top:0`**、`padding:5px 8px 3px`、12px／500 | 照它 |
| 選項 | 12px、`padding:6px 8px` | **`min-height:38px`**、圓角 10px、gap 8 | 照它 |
| 模型名 | 12px | **14px／500／行高 20** | 照它 |
| 「目前」 | 文字「目前」 | **打勾圖示**（`_7KE1Ra_check`：18px、`flex:0 0 18px`） | 共用圖示 `IconCheckOutline16`，拿不到才退回 ✓ |
| 空狀態 | `.dsh-tv-note` | `padding:10px`、13px／20 | 照它 |

教訓：**「照 DSH」要照到度量**。我先前只抄了「小字、灰階」這種形容詞，實際數字（38px 高、
14px 名、sticky 標題、4px 內距、打勾而不是文字）一個都沒對——那不叫模仿，只是看起來像。
## 2.6.26 — 選單那個錯誤的真正原因：undefined 穿過了 !== null 的守衛

使用者貼回我加的診斷（「模型目錄的形狀看不懂：Cannot read properties of undefined (reading 'model')」）
——那一行就是我需要的答案。

`modelMenu()` 讀目前選的是哪個模型時寫的是：

```js
chat.usage.route !== null ? String(chat.usage.route.model) : ''
```

**`undefined !== null` 是 true**，所以當 `route` 是 `undefined`（不是 `null`）時它會過關，
接著在 `undefined` 上讀 `.model` → 整個選單的 render 丟錯 → 被我的 try/catch 接住、顯示那一行。
（上一次「整頁空白」是同一個錯在沒有 try/catch 時的長相。）

修法：**兩個都要擋**（`!== null && !== undefined`），而且**在源頭就不要指派 `undefined`**
（`routeOfRecords` 的結果寫進 `value.route` 時也一起擋）。所有讀 `.route` 的地方都掃過一遍。

教訓（跟這幾輪同一條，但這次更細）：**「有守衛」不等於「守衛對」**——`!== null` 擋不住
`undefined`，而 JS 裡最容易漏的就是這個。
## 2.6.25 — 模型選單修好並重新打開（這次是讀原始碼，不是猜）

使用者：「直接一部收好順便連 css 風格這些都收好」。

### 上次為什麼會整頁空白

`modelMenu()` 讀目錄的形狀是**我照型別推的**，而推錯了：我寫 `group.provider`，
真實的 `ModelProviderGroup` 是 **`{ id, name, models }`**（模型則是 `{ id, name, description?, reasoning? }`）。
形狀一錯就在 render 裡丟錯，整棵樹跟著不見——就是「空白的一整頁」。

這次是**把型別宣告讀出來**才動手（`dsh-api-session-controller` 的 `ModelCatalog`／
`ModelProviderGroup`／`ModelCatalogModel`）：目錄是
`{ default, routableProviders, groups, failures }`。

### 除了修，還加了兩層

1. **整段選單內容包 `try/catch`**：以後形狀再怎麼意外，最壞是顯示一行錯誤，**不會整頁空白**。
2. **錯誤照實顯示**：目錄讀不到、切換失敗、形狀看不懂——都印出來，不靜默。

### CSS 一起收

選單的度量對齊 DSH 自己的 `._7KE1Ra_menu` 與 `.JObwrW_panel`：`max-height:min(360px,calc(100vh - 96px))`、
`padding:10px 12px`、字級 12／行高 20、項目 `padding:6px 8px`；讀取中／錯誤那兩行不再被撐開。
## 2.6.24 — 先把模型選單關掉（點開會整頁空白）

使用者：「選單點開是空白的一整頁」。

那是我寫的選單炸掉（整棵樹跟著不見），不是樣式問題。**修好之前先關掉**：那一顆 chip 現在只負責
**顯示**（這一間房目前用哪個模型），點它會出一行說明而不是開選單——不留一個按鈕在畫面上讓它炸。

下一輪要查的（照這幾輪的規矩：先讀、再在真頁面上量）：

1. `modelChip` 的 `onClick` → `loadModels()` → `chat.models` 這條路上的**形狀**：
   目錄回來的 `value.groups[].models[]` 我沒有看過真實回應（只從型別推），
   而 `modelMenu()` 直接讀它——**推的形狀錯了就會在 render 裡丟錯**。
2. `ModelChevron` 剛加、還沒接；`IconAlarmClockOutline16` 那三個共用圖示是驗過的。
3. 修好之後要當場驗「點開→看到清單→選一個→切換成功」四步，再打開它。
## 2.6.23 — 開始共用 DSH 自己的 UI 元件（第一步：圖示）

使用者：「麻煩依然根據 dsh 風格模仿，應該有很多共用資源的」＋「假裝是自用就可以了…做了一步先」。

### 查證（這一步就是為了確認它行不行）

- 酒館的 bundle 本來就拿得到 `require`（檔頭 `factory: (require) => { … require('react') }`），
  所以 `require('@deepseek-ai/dsh-client-ui-primitives')` 這條路一開始就是通的。
- 那個套件在磁碟上**沒有獨立目錄**（包在預建的 app bundle 裡、由 `__ModuleLoader__` 註冊），
  所以我沒有用猜的——**在真瀏覽器裡把它列出來**：**123 個匯出**，包含
  `Button`／`Modal`／`IconChevronDownOutline14`／`IconClockOutline16`／
  `IconDatabaseOutline16`／`IconAlarmClockOutline16`／`HoverCard`… 正好都是我手刻過的東西。
- `verify.mjs` 對 client 半只檢查檔案存在／語法／loader id 一致，**沒有 import 白名單**——
  所以在 client bundle 裡 require DSH 的 id 不會讓 repo 變紅。

### 這一步換了什麼

檔頭加一個**有守衛**的 require（拿不到就 `null`，全部回退到酒館自己那一份，不會壞），
然後把三個手刻圖示改成優先使用共用元件：碼錶 → `IconAlarmClockOutline16`、
資料庫 → `IconDatabaseOutline16`、時鐘 → `IconClockOutline16`。

真的頁面上驗過：那一排 pill 的 svg 已經是共用元件（六個圖形路徑的那一顆＝它的鬧鐘，
不是我自己畫的三個），`errors: []`，`primitivesAvailable: true`。

### 還沒做的（下一步）

- `ModelChevron` 已經寫好（優先用 `IconChevronDownOutline14`）但**還沒接上**模型 chip——留著，
  下一輪接。
- 接著換 `Button`（送出鍵與各顆按鈕）、`Modal`（面板／選單，連定位與關閉行為一起）。
## 2.6.22 — 模型 chip（DSH 那顆的位置與做法）

使用者：「一樣要盡量複製 dsh 的操作」＋「首先從選擇模型開始」。

### 位置與外觀

DSH 的 composer 那一列（`uV2eYG_trailing`）順序是 **model → 上下文環 → 送出**，所以 chip 放在
環的左邊（真的頁面上驗過：`chipBeforeRing: true`、`chipBeforeSend: true`）。外觀照
`_7KE1Ra_trigger`：28px 高、膠囊圓角、`padding:0 4px 0 8px`、字級 13／500、`gap:4px`，
右邊一個開啟時轉 180 度的 chevron（路徑也是抄它的）。選單往上開（跟其他面板同一條理由）。

### 做法（全部走公開的客戶端服務）

| 要什麼 | 用什麼 |
|---|---|
| 模型目錄 | `remote.session.modelCatalog()`——DSH 的 `ModelCatalogDirectory.load()` 就是這一個呼叫 |
| 換模型 | `sessions.selectModel({sessionId, provider, model, reasoningEffort?})` → `{ok}`／`{ok:false,error}` |
| 目前是哪個 | 開場快照的 `request/context`（`chat.usage.route`），跟畫面上其他數字同一個來源 |
| sessionId | 酒館自己的綁定表（`session.list`） |

**只讀公開服務、沒有 import 任何 DSH 的類別**——所以酒館的手寫 bundle 也做得到。

### 誠實說明

chip 本身在真的頁面上驗過了（位置、標籤、順序都對）；但**我這邊那間房的 session 沒有歷史**，
所以「點開選單」我**沒能在瀏覽器上驗成功**（`menuOpen: false`）。請你點一下看看：
目錄讀不到時它會照實顯示錯誤（不會靜默無反應）。若有問題，下一輪我會從 console 追。
## 2.6.21 — 一次只開一個面板（DSH 的操作也是這樣）

使用者：「一樣要盡量複製 dsh 的操作，現在一次我解開兩個摺疊就有點不像」。

輸入框那排（環／碼錶／資料庫）與訊息上的「用量」各自開了面板之後**可以同時開著** ✗。
DSH 不是這樣：它的 popupSelect／面板一次只會有一個。現在任一個打開時會把另一個收掉
（環／pill → 收訊息面板；訊息用量 → 收輸入框那排）。

（這一版是純客戶端，重新整理就生效。）
## 2.6.20 — 「本輪用量」搬到訊息上（真的位置），並記下提供方／模型

使用者貼了完整的「本輪用量」區塊，並說「還有這些資訊你剛才放錯位置了」。

上一版我把**用量／用时**放上訊息了，但**細節**（合計／提供方 · 模型／快取命中／未快取輸入／
快取讀取／輸出（其中推理）／本輪用時和速度）還留在輸入框那個面板裡 ✗。DSH 這一整組 locale
key 都是 `message.` 開頭（`message.turnUsage.*`、`message.ranFor`），所以**它們全屬於訊息**。

現在：點訊息上那一顆「**用量 … tok**」就展開那一輪的細節（再點一次收起，點畫面其他地方也收）。
輸入框那一排的面板只剩它的三份（上下文／會話統計／Token 用量累計）——不再混進「本輪」。

### 提供方 / 模型 也跟著訊息存

那一列的值是 `{provider, model}`：顯示時取自用量列的開場快照（`request/context` 事件），
發送時**一起記在訊息裡**（`chat.jsonl` 的 `extra.route`）——不然重新整理之後那一列就沒了。
宿主半的 `appendRoomMessages`／`readRoomMessages` 同步支援（跟 `extra.usage`／`extra.ms` 同一組）。

⚠️ 這一版動到**宿主半**（`extra.route`），所以要重啟 `dsh web`；重啟之後**新收到**的回覆才會
有「提供方 / 模型」。客戶端那半重新整理就生效。

### 另外回報的「沒法選擇模型和上傳檔案」

查過了：**酒館這一頁本來就沒有**那兩個東西（整份客戶端只有插圖／卡司那幾個 `type="file"`，
沒有附件鈕、也沒有模型鈕）——不是我改壞的，是還沒做。要做等於兩件新功能（見 `docs/plan.md`）。
## 2.6.19 — 訊息上那兩個標籤（用量／用时）＋ 跟著訊息存

使用者貼了 DSH 訊息上那兩個元素（`用量 22.6M tok`、`用时 3分28秒`）說「我是指這兩個位置」。

我先前把那兩件事放進**用量面板**（環／pill 點開的那個）✗——DSH 是把它們掛在**訊息上**
（locale key 是 `message.turnUsage.consumed` 與 `message.ranFor`，就在複製／讚／分支那一排
按鈕旁邊）。所以改成助理訊息氣泡**下面**那一行：`[資料庫] 用量 14.4K tok`、`[時鐘] 用时 3分18秒`
（兩個圖示也是照抄它的 path）。

### 要活得過重新整理，就得存進訊息裡

用量／用時是**那一輪**的事，所以跟著那一則訊息走：`chat.jsonl` 的 `extra.usage`／`extra.ms`
（SillyTavern 的 `extra` 本來就是自由欄位，酒館的 `reasoning` 已經住在那裡）。所以：

- **宿主半**（`workspace.js`）：`appendRoomMessages` 把 `usage`／`ms` 寫進 `extra`；
  `readRoomMessages` 讀回來。沒有東西時**不寫** `extra`（既有檔案的最小形狀不變）。
- **客戶端**：那一輪結束時把供應方回報的用量與實測用時一起附在訊息上（記憶體那份現在畫、
  檔案那份活下去）。
- ⚠️ 舊訊息沒有那兩個欄位——**不畫**，不是畫一個 0。

⚠️ 這一版動到**宿主半**，所以要重啟 `dsh web`：重啟之後**新收到**的回覆才會帶那兩個標籤
（在那之前的訊息沒有資料可畫）。客戶端那一半重新整理就生效。
## 2.6.18 — 點外面就收起、本輪的「提供方 / 模型」與速度

使用者：「現在要重複點擊才會縮回去，我應該點擊外面就會縮回去」，並把「本輪用量／本輪用時和速度」
那兩段貼過來說「這兩個參數順便」。

### 點面板外面就收起

那三顆按鈕自己的 `onClick` 只管切換；關閉改由一個 `mousedown` listener 負責（只在有面板開著時
掛、收起或卸載一定拆掉）。點在**面板／pill／環**上面不算外面——它們自己有 onClick，這裡再關
一次會變成「點一下開了又立刻關」。實際在頁面上驗過：點訊息區→收起 ✓、點面板裡面→不關 ✓。

### 本輪用量多一列「提供方 / 模型」

DSH 的 locale 寫著 `message.turnUsage.model`＝「提供方 / 模型」，值來自助理訊息的 `source`。
酒館的 `.jsonl` 沒那個欄位，但**開場快照的 `records` 是完整事件**——最後一則 `request/context`
就帶著 `provider` 與 `model`，順手撈下來（`routeOfRecords`，純函式、有測試）。沒有就不列。

### 「本輪用時」改名「本輪用時和速度」，並加上本輪輸出速度

DSH 那一節叫「本轮用时和速度」（`message.ranFor` ＋ `message.tokensPerSecond`），所以速度也
放進來：**本輪輸出 token ÷ 本輪秒數**。跟 DSH 同一條公式，差別只在分母——它用解碼時間，
酒館用整輪的牆上時間（串流沒有把解碼時間單獨報給客戶端），所以數字比 DSH 略低。
**寧可低估也不要假裝精確**，這件事寫在函式的註解裡。
## 2.6.17 — 每一顆按鈕開自己那一份（不是全部塞進同一個面板）

使用者：「我剛才是說顯示資料他會分多個按鈕分開顯示」。

我把四段（上下文／會話統計／Token 用量／本輪）全部畫進**同一個**面板 ✗——不管點哪一顆，
打開的都是那一份大雜燴。DSH 是**每顆按鈕一份**：點哪一顆就只看到那一顆的資料。

現在（真的頁面上點過、抄下來的）：

| 點什麼 | 打開什麼 |
|---|---|
| **環**（輸入框那一列，送出鍵左邊） | `上下文`：提示詞 1,065 tok／模型上限 1,000,000 tok／緩衝 998,935 tok／系統 141／工具 0／訊息 546 |
| **碼錶**（卡片外第一顆） | `會話統計`：輪 · 步 1 · 1／模型用時 3.1秒／首 token 平均（TTFT）0.5秒／輸出速度（TPS）200 tok/s（＋跑過一輪後的「本輪用時」） |
| **資料庫**（卡片外第二顆） | `Token 用量（累計）`：合計 1,180 tok／快取命中 78%／未快取輸入 143 tok／快取讀取 512 tok／輸出 525 tok（＋跑過一輪後的「本輪用量」） |

狀態從 `usageOpen`（布林）改成 `usagePanel`（`''`／`'ctx'`／`'stats'`／`'tokens'`）：點同一顆＝
收起來、點別顆＝換過去。**測試也跟著改**：三段各自驗「有自己那一份、而且沒有別人那一份」，
所以「不小心又全部混在一起」會紅。

⚠️ 順手記一個踩到的坑：pill 的 key 必須跟判斷式**一字不差**（我一度寫成 `'tok'` 對
`'tokens'`），症狀是「那顆按鈕點開只有一顆『重新讀取』」——空的。
## 2.6.16 — 環與統計分開（DSH 是兩件事，我擠成一排）

使用者：「你自己是分開顯示的」。

我把上下文、統計、token 擠成**三顆 pill 一排**、還整排塞進卡片裡 ✗。DSH 不是這樣分的，
它的前端就在磁碟上，這次直接讀它的 locale 與 CSS 對照：

| | DSH 自己的 | 這一版 |
|---|---|---|
| 上下文 | **`JObwrW_trigger` 圓環**：28×28、`border-radius:999px`、`display:grid`，在 composer 那一列的 `trailing` 裡、**緊貼送出鍵前面**（`aria-label="上下文已用 43%"`） | 一樣（環在卡片那一列，送出鍵左邊） |
| 統計 | **`.bOPqQW_root` 兩顆 pill**，在**卡片外面**、整排置中（`gap:12px`、`justify-content:center`） | 一樣（兩顆：碼錶／資料庫） |
| 面板 | 一個對話框（locale：`stats.dialog.title`＝会话统计、`stats.dialog.usageTitle`＝Token 用量、`stats.dialog.llmTime`＝模型用时、`stats.dialog.ttft`＝首 token 平均（TTFT）、`stats.dialog.speed`＝输出速度（TPS）） | 一樣（四段，點環或點 pill 都開同一份） |

所以那一排現在是：卡片裡 **`◯` ＋ 送出鍵**；卡片外面 **`⏱ 1 輪 1 步 · 200 tok/s`**、
**`🛢 1.2K tok · 快取命中 78%`**（真的頁面上量的）。

教訓跟上一輪同一條：**能讀原始碼就不要看畫面猜**——locale 檔直接把「哪一段叫什麼、
放在哪個對話框」寫清楚了。
## 2.6.15 — 連佈局也照 DSH：送出鍵挪到右下、面板四段（含「本輪用量」）

使用者：「盡量模仿甚至連同佈局也要，發送按鈕位置」，並把 DSH 那個統計對話框的內容整份貼過來。

### 送出鍵：右下角、34×34 圓形

DSH 的 `.uV2eYG_row` 是 `justify-content:space-between`：左邊一組工具鈕、右邊
（`.uV2eYG_trailing`）是模型鈕、計量環、然後**送出**——`border-radius:999px`、34×34、
往上箭頭、`transform:translateY(-2px)`，忙碌時同一顆變成停止。

酒館照做：檔案路徑移到**左邊**，右邊是圓形送出鍵（同一顆按鈕在忙碌時切換成「停下來」），
圖示就是 DSH 那一顆的 path。送出鍵的名字因此移到 `aria-label`（測試也改成找那個按鈕）。

### 面板：四段，照 DSH 那個對話框

| DSH 那段 | 酒館這版 | 來源 |
|---|---|---|
| 會話統計（模型用時／工具呼叫用時／首 token 平均 TTFT／輸出速度 TPS） | 一樣，再加「輪 · 步」 | `sessionStats` 投影 |
| Token 用量（合計／快取命中／未快取輸入／快取讀取／輸出） | 一樣（快取寫入不為零時才多一列） | `tokenUsage` 投影 |
| 本輪用量（合計／快取命中／未快取輸入／快取讀取／輸出（其中推理）） | 一樣 | **串流裡的 `{type:'usage'}` chunk** |
| 本輪用時和速度（本輪總用時） | 本輪總用時 | 按下送出→收到完整回覆 |

- 每一列都是「標籤靠左、數字靠右」（DSH 那顆環的面板用的也是 `margin-left:auto` 的數字）；
  面板裡的數字是**精確值加千分位**（它寫 `775,465 tok`），`12.3K` 那種精簡版只留在 pill 上。
- 時間格式照 DSH：不到一分鐘寫 `2.4秒`，超過寫 `152分41秒`／`2分01秒`。
- 「本輪」那兩段**只有跑過一輪之後才出現**——沒有的東西不編數字。

### 兩個在真頁面上才看到的問題（都已修）

1. **面板往下長會被切掉**：輸入框貼在對話頁底部，面板往下開一定被裁掉。DSH 的面板是
   `position:absolute;bottom:calc(100% + 8px)`——**往上開**。照做，並加上
   `max-height:52vh;overflow-y:auto`。
2. **「本輪用量」串流裡就有**：`StreamChunk` 的 `usage` 變體（`{type:'usage'}`）帶著供應方
   回報的這一輪用量；`tokenUsage` 投影是整份日誌的累計，回答不了這一輪。
## 2.6.14 — 輸入框變成一張卡（不再是四件分開的東西）

使用者：

> 當中有一些欺騙性的 UI，例如假裝是在同一個對話框，現在所有東西都是分開的。

問題不是哪一個元件不好看，而是**它們各自成一個單位**：textarea 自己一圈框、按鈕一列、
檔案路徑一行、用量 pill 又一列——四件東西疊在一起，看起來不像同一個輸入框。

DSH 的 composer 是**一張卡**（`.uV2eYG_card`，從磁碟上的前端讀出來的）：

- `border:0`——**沒有邊框**，靠 `background:var(--dsw-specific-input-major)` ＋ 柔和陰影
  ＋ `border-radius:22px` 圍出一張卡；
- 輸入區在卡片裡**沒有自己那一圈框**（`.uV2eYG_input`：`outline:none`、`min-height:52px`、
  `padding:4px 8px 0 14px`）；
- 按鈕列也在同一張卡裡（`.uV2eYG_row`：`padding:2px 8px 6px`、
  `justify-content:space-between`）；
- 送出是**圓形**主色按鈕（34×34、`border-radius:999px`）。

酒館照做（圓角走自己的 `--dsh-tv-radius-lg`；DSH 那邊是寫死的 22px）：`.dsh-tv-chatInput`
現在是一張沒有邊框、有底色、有圓角的卡；textarea 在裡面沒有框；按鈕列與檔案路徑在同一張
卡裡；**用量 pill 也放進同一張卡**——DSH 是把它放在卡片下方、靠寬度對齊「假裝」同一體，
這裡直接放進去，不必假裝。
## 2.6.13 — 那一列改成 DSH 真正的 pill（連度量一起抄）

使用者貼了 DSH 自己那排統計的**真實 DOM**，並說「還有很多微調工作要做」。

上一版只抄到「小字、灰階、不要框」，這一版把**度量**也抄了——`dsh-client-ui-chat` 的
`.bOPqQW_*` 與 `dsh-client-ui-conversation` 的 `.JObwrW_*` 就在磁碟上，直接讀出來：

| | DSH 自己的 | 這一版 |
|---|---|---|
| pill | `button`：`background:0 0`、`border:none`、`padding:1px 8px`、圖示與文字 `gap:6px` | 一樣（圓角走酒館的 `--dsh-tv-radius-pill`） |
| 一列 | `gap:12px`、**`justify-content:center`（置中）**、字級 13px／行高 20px | 一樣 |
| `·` | 自己的 span，左右各 6px | 一樣 |
| 圖示 | 16×16、`stroke:currentColor`、`stroke-width:1.25`（碼錶／資料庫） | **同一組 path**，直接抄 |
| 上下文環 | 14×14、半徑 5.5、`stroke-dasharray = 佔用 × 2πr`、`rotate(-90 7 7)` | 一樣（80%／95% 換黃紅是酒館加的） |

所以現在是三顆 pill：

```
[環]     上下文 1.1K / 1.00M（0%） · 緩衝 998.9K
[碼錶]   1 輪 1 步 · 200 tok/s
[資料庫] 1.2K tok · 快取命中 78%
```

點任何一顆展開面板：分段條（系統／工具／訊息）＋ 快取讀寫 ＋ 精確值 ＋「重新讀取」。

⚠️ **這一版的教訓**：能讀原始碼就不要看 DOM 猜度量。第一版是猜的（小字＋灰階），
第二版才是抄的——而且抄到才發現它**整排是置中的**、字級是 13px（不是 11.5）、
`·` 自己帶 6px 邊距、pill 其實是**按鈕**（`aria-haspopup="dialog"`）。
## 2.6.12 — 那一列照 DSH 自己的設計重做

使用者：

> 有顯示訊息但不夠好看，你可以參考一下 dsh 自己的 ui 是怎樣設計的。

第一版是**一排框起來的膠囊**（標籤一格、數字一格）——資訊量對，長相不對。DSH 自己那排
統計的規矩是：**小字、灰階、沒有框也沒有底色**；同一組的數字用 `·` 連起來
（`125 輪 1396 步 · 253 tok/s`），**組與組之間只留空白**；一眼看不完的細節收進**點開的
面板**（它那顆計量環就是這樣）。

所以現在是：

- 一行小字、四個「組」：`[條] 上下文 1.1K / 1.00M（0%）` ／ `緩衝 998.9K` ／
  `1 輪 1 步 · 200 tok/s` ／ `輸入 655 · 快取命中 78% · 輸出 525`
- 位置改到**輸入框下面**——DSH 那排也是掛在輸入框底下當註腳（輸入框是主角），
  而且要在 `.dsh-tv-chatInput` **外面**（那一塊有自己的上框線）
- 點一下展開細節：構成（系統／工具／訊息）、快取讀／寫各多少、精確 token 數，
  外加一顆「重新讀取」
- **「幾輪幾步、多快」是這一版新加的**，來源跟 DSH 自己那排一模一樣（`sessionStats`
  投影；速度＝解碼 token ÷ 解碼秒數）

測試也跟著改（`test-client.mjs` 14h）：現在釘「**不要膠囊／框**」、「摘要那一行不塞
細節」、「點開才看得到構成」，以及三個新數字。順手修掉一個測試自身的盲點：
`flatten()` 會用空白接起子節點，所以「`253 tok/s`」這種多節點拼出來的句子要先把連續
空白收成一個再比對。

## 2.6.11 — 對話頁的用量列，權限說明改成人話

使用者：

> 現在的對話框太簡陋了，並且權限描述都非常差。
> （要）上下文大小、使用、token、緩衝、命中這些，你照自己這個 webui 抄就好了。

兩件事都只動酒館自己的畫面，而且**純客戶端**（重新整理就有，不必重啟）。

### 輸入框上面多一列：這一間房到底用了多少

實際跑起來的樣子（真的房間、真的數字）：

```
上下文 1.1K / 1.00M（0%）  緩衝 998.9K  快取命中 78%
輸入 655（快取讀 512 · 寫 0）· 輸出 525  構成 系統 141 · 工具 0 · 訊息 546
```

| 那一格 | 意思 | 來源 |
|---|---|---|
| 上下文 | 下一個請求的提示詞規模／路由上限＋一條細進度條（80% 轉黃、95% 轉紅） | `contextPressure` |
| 緩衝 | 距離上限還剩多少 | 上限 − 用量 |
| 快取命中 | 提示詞有多少比例直接命中快取 | 快取讀 ÷ 輸入（**跟 DSH 自己那顆統計同一條公式**） |
| 輸入／輸出 | 整份對話的累計 token（含快取讀、寫） | `tokenUsage` 的三個互不重疊的桶 |
| 構成 | 系統／工具／訊息（啟發式，標「近似」） | `contextBreakdown` |

**三個鍵都是宿主算好、給瀏覽器讀的同一份投影**（DSH 自己那顆計量環讀它），酒館只是跟著
讀，所以不會出現「同一份日誌的第二個答案」。

### 中間走錯的一步（值得記下來）

第一版是**叫宿主半算**（`room.usage` op ＋ `ctx.tokenMeter` ＋ `ctx.sessions.get()`）。
它上線後那一列**一直是空的**——兩個原因都只在真實環境裡才看得到：

1. **`ctx.sessions.get()` 只看得到「現在活著的」session**。重啟之後那個對話不在記憶體裡，
   於是永遠量不到。→ 改成讀 session **串流的開場快照**（來自持久日誌，重啟照樣有）。
2. **舊綁定的 `chat` 欄位放的是顯示名稱**（新綁定才有 `room`）。用房間 id 比對永遠找不到
   → 現在兩種都比對。

順手把那一版的宿主半（`room.usage` ＋ `workspace.sessionIdOfRoom()`）**收回來了**：
純客戶端就做得到，不該讓它變成「要重啟才有」的功能。

### 兩個實測出來的 API 細節（照型別推會錯）

- `service.follow(...)` **不可以送 `assistantStream: false`**：宿主會用
  `session/follow rejected "request"` 把請求打回，而我們是靜靜吞掉的。
- 開場快照的 `projections` 在 **frame 自己身上**（`type: 'snapshot'`），**不是**包在
  `page` 底下。→ 測試原本照著錯的假設寫，所以**測試全綠、畫面全空**；現在測試釘的是
  真實形狀。

### 權限說明：寫「它拿到什麼」，不是工具名稱

以前是「只讀——read、glob、grep」——那是**實作**，不是使用者要決定的事。現在兩處
（⚙️ 設定 的酒館層、對話頁的「⚙️ 房間」分頁）都改成：

- 全關——它看不到你的檔案，也不能跑指令
- 只讀——它可以自己翻角色卡、世界書與對話紀錄，但不會改任何檔案
- 讀＋寫——加上可以新增或修改檔案（例如幫你寫世界書條目）
- 只讀＋上網——加上可以上網查資料
- 全部——跟一般 agent 一樣，能用的工具全開

## 2.6.9 — ＋ 變成來回鍵，房間裡就能改名

兩個都是**客戶端**的修正（重新整理頁面就生效，不必重啟 `dsh web`）。

### ＋ 原本是單程票

使用者：

> ＋ 這個按鈕會幫我跳轉到創建頁面，但有時我手多就係點想快速回去，重新點擊 ＋ 不會跳回去。

按下去會切到那間酒館的「💬 包廂」，但**人已經在包廂裡的時候再按一次什麼都不會發生**。

兩件事一起修：

1. **分區切換要通知主面板重畫。** 分區（`currentZone`）是模組層級的狀態，而重畫側邊欄
   **不會**重畫主面板——所以「已經停在這一頁」時按 ＋ 看起來像壞了。現在切分區之後
   借 `refreshChannel` 通知設定頁重畫（只重畫，不重讀：它的資料沒變），設定頁則用
   `useRefreshChannelRerender(renderPage)` 訂閱。這同時修好另一種情境：人停在
   「🎭 卡司」分區時按 ＋，以前畫面不一定會跟著切到包廂。
2. **再按一次＝沿原路回去。** 已經在包廂（同一個主面板、同一個分區、同一間酒館）而且
   有正在看的對話時，＋ 會把你送回那份對話。

判斷用「現在在哪裡」而不是「上次按了什麼」——後者要維護一堆清除點（換分區、點別的面板…），
漏一個就會把人送去奇怪的地方。為此多記一格 `shownPanel`：`layout.selectPanel()` 只有
setter，沒有 getter，側邊欄沒辦法問「現在主面板是哪一個」。

### 房間裡就能改名

使用者：「改名應該房間內都可以改，不一定要在出面」。以前只有側邊欄那一列的 ⋯ 選單能改，
人已經在房間裡卻要退出去才改得動名字。現在「⚙️ 房間」分頁第一格就是名字 ＋「儲存名字」
（Enter 也可以）。

順手修掉兩個同型的 bug：

- **`room.rename` 的回傳值是薄的**（`{character, room, name}`），而兩處都直接用它**取代**
  手上的座標——`roomPrompt`／`allowTools`／`file` 當場消失，那一頁的欄位就空了。
  改成合併（`Object.assign({}, 舊的, 回傳的)`）：改名不是重讀，其他欄位沒有理由被清掉。
- **對話頁的身分原本用「角色/名字」**，所以在房間裡改個名會把這一頁整個重置
  （訊息、草稿、通知全部清掉）。改成用**房間 id**——改名前後還是同一間房。

## 2.6.8 — 房間的圖真的住進房間，縫也封完了

### 房間的插圖搬進 `<room>/art/`

```
chats/<角色>/<roomId>/
├── room.json
├── chat.jsonl
└── art/          ← 圖現在真的落在這裡
```

**只改了一個地方**：`lib/assets.js` 的 `assetDir()`。因為讀圖路由（`resolveAssetPath`）、
列出（`listAssets`）、上傳（`writeAsset`）、刪除（`deleteAsset`）**最後都呼叫它**
——所以 URL 的形狀完全不變（`/api/dsh-tavern/assets/chats/<角色>/<房間id>/<檔名>`），
客戶端只有一個地方要改（對話頁的 `AssetManager` owner 從名字改成**房間 id**）。

特例只在 owner **有兩層**時生效（`角色/房間id`）；只有一層時走原本那條路，所以既有的
`art/chats/<角色>/…` 不會被指到不存在的地方。**舊圖不搬**（動使用者的圖檔風險比較大），
它們還在磁碟上，只是不會顯示。

### 直接後果：`counts.art` 曾經少算

房間的圖離開 `art/` 之後，只掃 `art/` 子樹的計數**數不到它們**——大廳那個「插圖」數字
從 4 掉到 3。測試先紅了才發現。現在**兩邊都數**（`countArt` ＋ 新的 `countRoomArt`）。

### 縫封完了：`resolveRoom` **只認 id**

2.6.6 的 `resolveRoom` 收「房間 id **或**顯示名稱」——那條 fallback 就是三個 bug 的源頭
（同名可以有兩間房，依名字解析會命中第一間）。現在**只認 id**：送名字會**明確報錯**，
不會默默寫進別的房間。

`room.list` 的角色參數也改成**可省略**（不帶＝列整間酒館的房間）——那正是客戶端要的形狀，
也是拿掉 `chat.*` 相容層的前提。

### 改名：`listChats` → `listAllRooms`

那個名字從 2.6.6 起就名不副實（它列的是**房間**）。10 處一次改完（定義 1、呼叫 5、
註解 2、文件 1）。純改名，行為不變。

### 已知、刻意留著的

`lib/index.js` 裡還有 6 個 `chat.*` 的別名（與 `room.*` 等價）。要收掉得改**約 70 處**
（`lib/client.js` 15 ＋ `test-client.mjs` 43 ＋ `smoke.mjs` 12），而它**已經不是正確性問題**
——危險的那一半就是上面那個 `resolveRoom`。所以這是**命名衛生**，留給一次專門的掃描。

## 2.6.7 — 封掉新舊形狀的縫，然後打掃

2.6.6 的房間佈局上線之後，使用者連續回了三個 bug，**三個都是同一個決定造成的**：
為了「客戶端一行都不用改」而做的 `chat.*` 相容層（收 id **或**顯示名稱）。
那個決定救了 19 個呼叫點的改動，代價就是新舊形狀交界處的縫。

### 修掉的三個

| 症狀 | 原因 |
|---|---|
| 進房就報「讀取對話失敗：**房間 不可為空**」 | 包廂建立後直接進去時，手工拼的物件**漏了 `room`** |
| **每間新房的訊息都跑進同一個對話框** | 客戶端送的是**顯示名稱**，而 `resolveRoom` 依名字會命中**第一間**同名房。預設名稱就是角色名，所以同名是常態 |
| 建完房要**重新整理**才看得到 | 側邊欄訂閱的是「只重畫、不重讀」那個版本——它的前提是「這個通道只承載狀態變了」，但同一個通道也承載「內容變了」 |

**修法**：客戶端所有訊息／刪除／改名／綁定**一律送房間 id**（七處 ＋ 分支那兩處）、
`openChat` 帶 `room`、側邊欄收到通知後**重讀展開中的酒館**。

### 新：📂 打開資料夾

📄 檔案 分頁的主鍵從「複製路徑」換成「**帶你去**」——走 DSH 原生的
`GET /open-in-app/apps` → `POST /open-in-app/open`（`{app, path}`），**純客戶端、不用重啟**。
優先挑檔案管理器（`explorer`／`finder`／…），認不得才退回清單第一筆——拿第一筆去開資料夾
可能開成編輯器。

### 預設名稱加上隨機尾巴

`createRoom` 沒給名字（或只給角色名）時，顯示名稱變成 **`老闆娘-a7f2`**（後綴就是 id 的
隨機碼）。以前每一間都叫「老闆娘」，清單上完全分不出誰是誰——使用者：「預設+隨機這些基本」。
自己取的名字原樣保留。

### 打掃：249 行死碼

- 五段測試（約 300 行）全部搬到房間 API。**斷言一條都沒少**，而且兩處**語意反轉**之後
  換成更強的斷言：改名從「四處都搬對了」變成「**四處都沒動**」（`room.json` 改了、
  資料夾／對話檔／插圖／綁定全部留在原地），撞名從「自動編號」變成「**同名不衝突**」。
- 舊的 `createChat`／`readChatMessages`／`appendChatMessages`／`renameChat`／`deleteChat`
  （**249 行**）刪除。刪法是先 grep 確認全 repo 零命中，再用一次性腳本按縮排慣例移除
  ——安全性由測試保證（跑前跑後都全綠）。
- ⚠️ 那支腳本第一次跑**失敗得很有教育意義**：它回報「找不到收尾、移除 0 個」，因為
  `lib/workspace.js` 是 **CRLF**，按 `\n` 切之後每行尾端還留著 `\r`，`'  }'` 永遠比不中。
  修好行尾比較才重跑。

### 順手抓到的一個真缺口

`renameRoom` 原本直接呼叫 `writeRoom`，而那一支會**順手 `mkdir`**（給 ⚙️ 房間 分頁的防禦）
——於是「對不存在的房間改名」會**默默把它建出來**。改名的前提是它存在：現在先 `resolveRoom`。

## 2.6.6 — 一間房＝一個資料夾

使用者：「房間自己也有資源所以不能用一個檔案放，應該用文件夾，所以建立的時候最好直接用 id，
這樣方便修改也要給我修改位置」「有些設定應該是在房間中設定而不是在酒館中設定」
「可以刪除重新來」。

### 佈局

```
chats/<角色>/<roomId>/
├── room.json      這一間房的設定（人看得懂、可以手改）
├── chat.jsonl     對話本身（SillyTavern 格式不變）
└── art/           這一間房的插圖，跟著房間走
```

**身分是 `roomId`（建立時固定，之後永不變），不是名字。** 這一條解掉的是舊佈局最痛的地方：
房間的資源散在 `chats/<角色>/<名>.jsonl` 與 `art/chats/<角色>/<名>/` 兩棵樹上，兩邊都用
**名字**當路徑——所以改個名字要同時搬四處（檔名、`.sessions` 對照表、插圖資料夾、
`tavern.json` 的主圖 key）。現在**改名只改 `room.json` 裡的一行，不動任何路徑**。

id 的形狀是 `時間 base36-隨機 4 碼`（例：`m1k3x9-a7f2`），用 `mkdir`（**不帶** `recursive`）
當獨佔鎖——目錄已存在會丟 `EEXIST`，撞到就換一個。可排序、刪掉不會有空號、不必查表。

### 每間房自己的設定

| 設定 | 放哪 | 為什麼 |
|---|---|---|
| `roomPrompt`（這一場的指示） | **房間** | 「這一場下著雨」不該是全域 |
| `allowTools`（工具權限） | **房間**，`inherit`＝聽酒館的 | `tools.restrict()` 本來就是 per-agent＝per-session |
| `userPersona` / `tavernPrompt` | 酒館 | 「你是誰」與「這間店」跨房間不該變 |

agent 面在每一輪讀 `room.json`（`roomField`）並**蓋過**酒館那一層。**全部留空時提示詞與
以前一字不差**——每一層都保留「沒設＝沿用」那條路。

### 為什麼有一個 `chat.*` 相容層

客戶端有 **19 個呼叫點**（包廂／卡司／側邊欄／對話頁）。把 op 直接換成 `room.*` 再改客戶端，
**改一半就是「新建的房間打不開」——那不是紅的測試，是壞掉的介面**。所以改成：

- `chat.list` 回傳**房間**（形狀與舊的對話一致，多一個 `room` 欄位）
- 其餘的 op 用 `resolveRoom` 收「房間 id **或** 顯示名稱」

客戶端一行都不用改，全程可用。這是暫時的：客戶端切完就只剩 id 那條路。

### 語意有兩處**刻意改變**

| | 舊 | 新 |
|---|---|---|
| 同名 | 自動編號 `初次見面-2` | **同名不衝突**（身分是 id，兩間房各自存在）|
| 刪除 | 只刪 `.jsonl`，插圖留在 `art/chats/` 變孤兒圖 | **整個資料夾刪掉**（房間的資源跟著房間走）|

兩條測試因此被改寫——而新的斷言**更強**（釘 id 唯一、資料夾佈局、fail-closed），
不是把斷言拿掉讓它變綠。

### 對話頁四個分頁

💬 對話 / 🖼️ 插圖 / **⚙️ 房間** / **📄 檔案**。

- **⚙️ 房間**：這一場的指示 ＋ 這一間房的工具權限（權限選了就寫檔，因為它是安全開關）
- **📄 檔案**：**房間資料夾的絕對路徑**＋「複製路徑」（使用者：「也要給我修改位置」）
  ——沒有剪貼簿就照實說，不假裝複製成功

### 踩到的坑

- **`deleteRoom` 先 `rm` 資料夾、才讀 `room.json` 拿顯示名稱** → 讀到預設值 → 名稱那條比對
  永遠不成立。症狀是「刪掉了，但說沒有解掉任何 session」，看起來像綁定比對寫錯。
  **先讀、再刪。**
- **`bindSession` 是明確列出欄位的**（不是白名單函式，但同一種後果）→ `room` 被丟掉 →
  連帶讓每房設定永遠讀不到。新增欄位一定要回來加。
- **`chat.list` 不帶角色**，一開始接到 `listRooms(character)` 就直接丟錯——它要的是
  「列出所有角色的房間」。

### 不做遷移

使用者同意「刪除重新來」，所以**不做遷移層**：新的程式只認房間資料夾這一種形狀，
舊的 `<名>.jsonl` 留在磁碟上不動（不刪、不搬、不讀）。這樣少了「兩種形狀並存」的分支
——那正是這類改動最容易長 bug 的地方。設計文件：`docs/room-layout.md`。

## 2.6.5 — 大廳變成大廳、設定自成一個分區、工具權限分級

使用者：「我實際上想模擬的視覺是一個真的酒館大廳，而不是一堆設定集」
「大廳要讓用戶看見有什麼房間和什麼角色」「其他東西應該進進階設定」
「還有他會不會有讀取文件的能力？我是不是能夠設定權限？」

### 分區：五個，設定自己一個

- **⚙️ 設定**是新的第五區：改名、圖示、你的名字、persona、這間店的規則、備註、
  店面圖、資料夾說明、（收合的）移除酒館。**大廳不再放任何設定。**
- **🏠 大廳**只剩「這間店有什麼」：四個數字 → 一行指路 → 這裡有誰（角色卡片）
  → **房間**（真的對話清單，點一張卡直接進去）。
- 那三個入口按鈕（包廂／卡司／藏書）拿掉了——分區列就在上面，不需要再放一次。

### 房間是卡片，不是一行文字

房間用 **16:10 橫式**卡面（角色是「人」用 3:4 直式，房間是「地方」用橫式），
格線與名稱列跟卡司的海報牆同一組。沒有房間插圖時退而用**那個角色的主圖**，
都沒有才畫一個**自己畫的 SVG 門**——不是 emoji：emoji 在不同平台的形狀、大小、
基線都不同，塞進固定比例的卡面就會忽大忽小（使用者：「🚪根本就不貼合」）。

### 工具權限：每一間酒館自己選，預設全關

`tavern.json` 的 `allowTools`：`none`（預設）／`read`／`write`／`web`／`all`。

- DSH 的 `tools.restrict()` 是 **per-agent** 的——它要求 `agent.ctx`，在全域 context
  會直接丟錯（DSH 自己的註解：「要擋就擋那個 agent，不要擋全部」）。所以
  「這一間店的角色能不能讀檔」做得到，而且每個 session 各自算。
- 載入當下還不知道這個 agent 屬於哪間酒館，而 `agent/pre-step` 的 payload 每一輪
  都帶著 `agent`。但**不能多掛一個 hook**：`test-agent.mjs` 的 R5 是
  `deepEqual(seen.events, ['agent/pre-step'])`——完整清單。所以套用等級走那個
  本來就每輪都跑的 `systemPrompt.variable` 回呼，**一個 hook 都沒多**。
- 預設那一級走**原本那條** `denyInheritedTools()`：它自己會排除保留的 `run_code`
  （`restrict()` 拒收那個名字，另一條測試就在釘這件事）。其餘等級用白名單
  `{ allow: [...] }`——不必知道 DSH 現在有哪些工具，而且**漏列＝關著**。
- `restrict()` 的限制是**交集**，重複套用只會越縮越緊，所以記住 `applied`，
  等級變了才換，換之前先解除舊的。
- 套用器宣告在 `apply()` **裡面**，不是模組層級：這個面是每個 agent 各掛一次，
  放模組層級的話 A 店選「只讀」會蓋掉 B 店的「全關」。

### 輸入：persona 與「這間店的規則」

⚙️ 設定 多了兩欄，agent 面把它們**接在角色卡後面**（不是取代）：`{{user}}` 的
persona 與這間店的通用規則。**兩欄留空時提示詞與以前一字不差**——「按預設來就可以」。

⚠️ `writeSettings` 是**白名單**：沒列到的欄位會被默默丟掉（`assets` 就是這樣壞過一次）。
`userName`／`userPersona`／`tavernPrompt`／`allowTools` 四個新欄位都補進去了。

## 2.6.4 — 對話頁、匯出、以及「變數真的有用」

起點是使用者的一句：「我們目的是要有一個好看的 UI，並且還有很多設定要在包廂內設定」，
以及後來的「變量應該是也要有用的，例如用戶名稱我在哪裏設定」。

### 對話頁：像通訊軟體

- **每一則訊息＝頭像 ＋ 氣泡。** 角色的頭像就是那張卡的**主圖**（`useTavernData` 已經把
  `character.list` 讀進來了，所以沒有多打任何一次 RPC）；你這邊沒有個人圖，用名字的
  第一個字。頭像是**圓的**——設計語言的圓角檢查裡 `border-radius:50%` 是唯一允許的例外。
- **名稱在氣泡外面、上面**，氣泡裡只放內容。
- 順手修掉：`.jsonl` 裡沒名字時宿主半寫的佔位字串 `unused` 以前會**直接顯示在畫面上**，
  現在顯示「你」。
- **分頁**：💬 對話／🖼️ 插圖／📄 檔案，跟四個分區同一組樣式。以前插圖管理器與檔案路徑
  全部疊在訊息下面，對話一長就被推到看不見。
- 分頁內容是**同一層的兄弟節點**，不是再包一層：`.dsh-tv-chatLog` 的高度靠父層 flex
  撐出來，多包一層會讓捲動高度算錯。

### 匯出：一個檔案就是一張卡

卡司的編輯器多了「📤 匯出 PNG 卡」與「📤 匯出 JSON」。

- **完全在瀏覽器半做完**，沒有新增任何宿主路由或 RPC。圖已經在瀏覽器裡、卡片資料也讀過了，
  繞去 Node 再回來等於把好幾 MB 搬兩趟；而且宿主半的新路由要重啟 `dsh web` 才生效
  ——那正是插圖折騰兩輪的原因。
- PNG 卡＝主圖 ＋ `ccv3` 區塊，**接區塊不是重新編碼**（`IDAT` 一格都不動）。
- 同一套格式知識現在有兩份（客戶端與宿主半），用**跨面契約測試**釘住：拿客戶端產生的
  位元組交給宿主半的讀取端讀，只有 base64、CRC、chunk 佈局全對才會一致。

### 卡片編輯器：補齊 v2／v3 的欄位

原本 8 個欄位，**缺 7 個**。補上 `nickname`、`creator_notes`、`tags`、
`alternate_greetings`、`group_only_greetings`、`creator`、`character_version`。

- 最嚴重的是 **`alternate_greetings`**：「＋ 新對話」的開場白選單**就是讀它**，
  而編輯器編不了它——那個功能等於只能靠外面的檔案餵。
- `group_only_greetings` 是規格裡**寫死 MUST** 的欄位，開出來才進得了檔案。
- 陣列欄位在畫面上是「一行一個」，存檔時切成**陣列**（存成字串的話別的前端讀到的是
  字串而不是清單，我們自己的開場白選單也會讀不到）。
- **刻意不開**：`source`、`creation_date`／`modification_date`（規格說 SHOULD NOT 由
  使用者編輯）、`assets`（插圖管理器負責）、`character_book`、`extensions`。

### 變數真的有用：`{{user}}` 與 `{{char}}`

替換引擎（`substitute`）本來就在，只是 `{{user}}` **永遠是預設的「你」**——沒有任何地方
可以設它。現在：

- 大廳多了「你的名字（`{{user}}`，存在 `tavern.json`）」。放酒館層級而不是每個對話，
  因為那是「你是誰」。
- 讀取優先序：agent 面的 `settings.user` → `tavern.json` 的 `userName` → 預設「你」。
  讀不到一律回空字串，**使用者名稱不該有辦法讓一整輪對話失敗**。
- `{{char}}` 改用 v3 的 `nickname`（規格說 SHOULD 用它而不是 `name`；留空就退回 `name`，
  所以 v2 的卡片行為不變）。

⚠️ `writeSettings` 是**白名單**：沒列到的欄位會被默默丟掉（`assets` 就是這樣壞過一次）。
新欄位一定要記得加進去。

### 建立對話之後直接進去

以前包廂那一列**只有「刪除」、沒有任何進去的方法**，而建立完也只是回到列表。
現在建立完直接跳到那間房，列表每一列也都可以點。

- 導覽刻意**就地寫**、不抽成共用小工具：`currentChat` 宣告在外層，而 `chatKeyOf`／
  `bumpActiveVersion`／`selectPanel` 都在內層，放外層的小工具會看不到它們
  （實際下場：`chatKeyOf is not defined`，被 `.catch` 吞掉、變成一個錯誤框，
  看起來就只是「沒有跳過去」）。**這個作用域陷阱絆倒過兩次**，所以純函式一律放外層。
- 建立完的斷言會把**錯誤框的內容**放進訊息裡，所以「被 catch 吞掉」不會再裝成「沒反應」。

### 範例卡只有一個檔案

`samples/characters/` 以前同時放 `.png` 與 `.json`，看起來像「分開儲存」——而這個插件的
立場正好相反：**PNG 是交換格式，`characters/<id>.json` 才是儲存格式**（見
`docs/design-comparison.md`）。那份 `.json` 只對人類有用，所以拿掉了；驗證改成直接跟
唯一來源 `defaults.js` 的 `sampleCard()` 比對。

## 2.6.3 — 包廂的「新對話」變成一張設定卡

使用者：「也要讓我選擇卡片，我們目的是要有一個好看的 UI，並且還有很多設定要在包廂內設定」。

以前包廂的「＋ 新對話」只是一串**名字**，點下去就建檔。但「開一份對話」其實要決定
三件事，而且第三件之前完全沒有被用到：

| 欄位 | 以前 | 現在 |
|---|---|---|
| 跟誰 | 純文字清單 | **海報牆式的選卡**（有主圖用主圖、沒圖用佔位符，選取狀態是 accent 外框） |
| 對話名稱 | 不能填（就是角色名的檔名） | 可以填，撞名自動變 `-2` |
| 開場白 | **完全沒用到** | 可以選卡片的第一則／`alternate_greetings`／不要，並即時預覽 |

### 幾個刻意的決定

- **開場白是卡片裡本來就有的資料。** `first_mes` 與 `alternate_greetings` 從第一版就
  存在於每張卡裡，卻沒有任何地方讀它們——開了對話就是一片空白。現在選定的那一則會用
  `chat.append` 寫成這份對話的**第一則訊息**（`isUser:false`），所以模型與歷史都看得到。
- **名稱跟著卡走，但不蓋掉手改過的。** 換卡時若名稱還是上一張卡的預設值就一起換；
  使用者打過字之後就不再覆寫——改名是使用者的決定，不該被下一次點選吃掉。
- **`append` 用的是 `chat.create` 回傳的名字**，不是自己送進去的：撞名時 `createUnique`
  會換編號（`初次見面-2`），用自己那個名字會 append 到一個不存在的檔案。
- **不做「你的名字」。** 標頭裡的 `user_name` 全 repo 只有一個寫入點、**沒有任何讀取點**
  ——做一個改了也沒作用的欄位，比沒有這個欄位更糟。
- **沿用既有的樣式與元件**（`.dsh-tv-field`／`.dsh-tv-in`／海報的 `.dsh-tv-poster`），
  只新增兩條 CSS：選卡用的密度（`minmax(96px,1fr)`）與選取狀態（負 `outline-offset`
  畫在內側，所以不會推動版面）。顏色一律走 `--dsh-tv-*` token。

### 測試

`test-client.mjs` 新增 4k：八個情境，涵蓋「有圖用圖」、預設選第一張、名稱跟著卡、
手改過不被蓋、換卡要把開場白退回第一則、沒有 `alternate_greetings` 就不該有「其他 1」、
`append` 用的是 `create` 回傳的名字、以及「不要開場白」時**不該有任何 append**。

順帶記一個踩到的坑：這個測試的重繪**不可以** `resetHooks()`。`MapChatFiles` 的狀態住在
`React.useRef` 裡，重置 hook 槽等於把那個 ref 清掉，`picking` 跟著歸零、表單永遠打不開
（症狀是斷言拿到空陣列，看起來像按鈕沒 render，其實是狀態被測試自己清掉了）。

## 2.6.2 — 圖真的送出來了（修正做在對的那一半）

2.6.1 修掉了宿主半的註冊路徑，但**已經在跑的那台 `dsh web` 是舊的**：宿主半只在
行程啟動時載入一次，改檔案對它沒有用。使用者的回報因此是「圖還是不顯示」。
當時那份宿主半回報的版本是 `tavern-2.5.1`——比這一輪的四分區介面還舊。

真正的修正必須在**瀏覽器半**（那一半靠重新整理就生效）：

- 新增 `assetSrc()`，把宿主半給的資產 URL 改成 `…/assets//…` 的形狀。
  DSH 的 prefix 比對是 `pathname === prefix || pathname.startsWith(prefix + '/')`
  ——**它自己會補一個斜線**，所以註冊成 `…/assets/` 的舊宿主只吃得進雙斜線。
  而這個形狀**新舊宿主都吃**：
  - 舊（註冊 `…/assets/`）→ `startsWith('…/assets//')` ✓
  - 新（註冊 `…/assets`，2.6.1 起）→ `startsWith('…/assets/')` ✓

  它是**唯一同時相容兩者的 URL 形狀**，所以這個修正不會在宿主升級後反過來壞掉。
- 套用點是 URL 進入 `<img src>` 的邊界：`findAssetUrl()`（海報牆、世界書、對話列）
  與插圖清單的兩個 `<img>`。
- 等沒有舊宿主在外面跑之後，`assetSrc()` 可以整支拿掉。

### 驗證

從真瀏覽器頁面打同一個檔案的兩種形狀（就是使用者抱怨的那張圖）：

| URL | 結果 |
|---|---|
| `/api/dsh-tavern/assets/characters/…/….png` | **404** |
| `/api/dsh-tavern/assets//characters/…/….png` | **200，4,227,380 位元組** |

教訓：**「檔案在不在」跟「送不送得出去」是兩件事。** 先前兩輪都在查路徑與權限
（檔案確實在、權限也對），但請求根本沒進到我們的路由；而 DSH 的 fallback 對
curl 回 401、對瀏覽器回 `text/plain` 的 404，兩個都不是我們的回應，
所以「錯誤訊息」從頭到尾沒指向真正的原因。診斷的關鍵是**註冊路徑與「DSH 怎麼比對」
放在一起看**，而不是只看自己那一端。

`test-client.mjs` 的 4h 也從「比字串」改成「看海報牆真的畫出來的 `<img src>`」。

## 2.6.1 — 插圖從來沒有被送出去過（一個字元的 bug）

### 修：所有插圖靜默 404

`webServer` 的 prefix 比對是

```js
pathname === prefix || pathname.startsWith(prefix + '/')
```

它**自己會補一個斜線**。而我們註冊的是 `/api/dsh-tavern/assets/`（結尾帶斜線），
比對就變成 `startsWith('/api/dsh-tavern/assets//')`——永遠不成立。

於是：路由註冊成功（`smoke.mjs` 也檢查了「有註冊」）、沒有錯誤訊息、
但**一張圖都送不出去**。請求落到 DSH 自己的 fallback：從瀏覽器看是 `text/plain` 的
404，從命令列看是 401（未知的 `/api/*` GET 會先被擋 auth）。
症狀看起來完全是「圖不存在」，所以先前一直在查檔案路徑與權限，方向錯了。

- `index.js` 的 `TAVERN_ASSET_PATH` 拿掉結尾斜線。
- `assets.js` 的 `ASSET_ROUTE_PREFIX` **保留**結尾斜線——它是拿來串 `<img src>` 的，
  兩個常數刻意不同，兩邊都加了「不要合併」的註解。
- `smoke.mjs` 加的守衛不是比字串，而是**用真實的資產 URL 形狀跑一次那條比對規則**：
  註冊路徑必須能被 `/api/dsh-tavern/assets/characters/角色/立繪.png` 比對到。

### 新：PNG 角色卡可以寫了（`writeCardIntoPng`）

`lib/pngcard.js` 原本只讀不寫。現在多了寫入端，用途是「把自己的人物卡匯出成一份
可以分享的檔案」。

- **接區塊，不是重新編碼**：`tEXt` 插在 `IEND` 之前，`IDAT` 的像素資料一格都不動。
  畫質零損失，而且不需要任何影像函式庫（JPEG 做不到這件事，這也是要求來源圖必須是 PNG 的原因）。
- 區塊內容是 **base64(UTF-8 JSON)**，不是原始 JSON。踩過：直接塞 JSON 不會在寫入時報錯，
  只會在**讀**的時候變成亂碼，錯誤訊息完全指不到原因。
- 原本就有的卡片區塊會**留著**：`ccv3` 優先的規則由讀取端決定，寫入端不該改寫別人的資料。

### 新：`test-pngcard.mjs`

`lib/pngcard.js` 是匯入人物卡那條路上唯一**完全沒有測試**的檔案，而它踩的坑
（base64）只有在真的拿一張卡去匯入時才會炸。這個套件自己造 PNG，不依賴 fixture，
八個區塊涵蓋：檔頭、走 chunk、`ccv3` 優先與大小寫、base64、寫入不重編碼、
CRC 正確性、長度謊言與截斷檔、缺卡時的錯誤訊息。

## 2.6.0 — 四分區：一間酒館＝一張面板，四個分區在裡面

使用者：「UI 都非常簡陋，給（缺）一點現代化以及為了這個項目專門設計的味道」。
這一版是那個要求的**資訊架構**那一半（視覺契約在 `docs/design-language.md`）。

### 主面板變成四個分區（`redesign.md` §3.2）

一間酒館仍然是**一張** DSH panel（`main:tavern`）——DSH 不需要知道酒館裡有幾個
分區；分區切換、清單、編輯器全部是我們自己的程式：

| 分區 | 做什麼 |
|---|---|
| 🏠 大廳 | 概況、**快速入口**（帶著數量跳去其他三區）、店面圖、名稱／圖示／備註 |
| 💬 包廂 | 對話清單、開新對話、刪除 |
| 🎭 卡司 | **海報牆**（找卡）→ 編輯器（改卡） |
| 📖 藏書 | **條目清單**（改一本書的條目）＋ 原始 JSON 模式 |

### 幾個刻意的決定

- **`currentZone` 用模組層級的變數，不是 `useState`**：這個面板是常駐的
  （切去別的 panel 再回來、或換酒館，不該被重置回大廳）；而且離線測試的假 React
  把 `useState` 的 setter 做成空的，靠它的狀態轉移在那裡驗不到。
- **「移除」收進收合的「進階」**：它以前就貼在「重新命名」右邊，而那句
  「只會從清單拿掉、不會刪檔」在頁尾——兩者隔了整個畫面。現在收合時
  按鈕**根本不在 DOM 裡**。
- **卡司的「找卡」與「改卡」分開**：還沒選卡時是海報牆（主圖＋名字），選了才進
  編輯器，而且有返回鍵。以前是 230px 的窄清單貼著編輯器，主圖根本看不出來是誰。
- **藏書的條目編輯器只做常用欄位**（標題／關鍵字／常駐／內容），而
  `uid`／`order`／`selective`／還不認識的欄位**原樣保留**；V2 的 `keys` 也不會被
  我們擅自改成 `key`（那是別人檔案的格式）。要精確控制排版時走原始模式。
- **`.dsh-tv-sec` / `.dsh-tv-secTitle` 以前根本沒有 CSS**，分區標題長得跟瀏覽器
  預設的 `h3` 一樣——那是「簡陋感」的具體來源之一。

### 裝修：`style.bubble` 與 `custom.css`

使用者原話：「我記得叫你設計一種格式讓我能夠透過目錄來修改，例如背景顏色、
對話框風格之類的」。**顏色那一半早就有**（`theme.json` 的 token），
**形狀那一半沒有**——token 只能換數值，換不動形狀。所以補了兩層：

- **`style.bubble`**：`bubble`（預設）／`plain`（沒有氣泡，像小說排版）／
  `tail`（帶尾巴）／`paper`（紙張感）。四個值是**列舉**，打錯會落回預設
  **而且回報**在 `dropped` 裡——默默用預設會讓使用者以為自己改了。
- **`custom.css`**：token 與樣式都改不到的（背景圖、材質、動畫）走這一層。
  它**排在元件樣式後面**才注入，所以蓋得過去；而且**包在
  `@scope (.dsh-tv-view)` 裡**——SillyTavern 的 `* { text-shadow }` 污染整個宿主
  是前例，不能讓使用者的 CSS 直接進 `<head>`。

實作上的一個關鍵取捨：**氣泡樣式走 CSS 變數，不是 class**。元件樣式是靜態的一層，
而主題是「換一間酒館才換一次」；用變數就只要換那一層，不必讓每一顆氣泡知道自己是
哪一種樣式，也不必在畫面上掛多餘的 class。

⚠️ `style` 與 `custom.css` 都要**宿主半**（`theme.read`）回傳，所以
**要重啟 `dsh web` 才生效**——`theme.json` 的 token 本來就是這個規矩。

文件補在 `README.md` 的「裝修」一節：兩層的分工、最常改的 token 對照表、
四種氣泡樣式、以及 `@scope` 的規矩。**那裡以前只有一行**——機制做好了
卻沒人知道怎麼用，這是同一件事的另一半。

### 順手清掉 223 行死碼

`lib/client.js` 裡有三處同名的頂層宣告，而 JS 的規則是「同一個作用域裡**最後一個
宣告勝出**」——所以前面那一份永遠不會被呼叫，**改它不會有任何反應**。
刪掉的是 `reasoningDeltaOf`、`ASSET_LABELS`／`ASSET_LABELS_BY_DIR`、以及一整塊
圖示（`IconEdit`／`IconBranch`／`IconTrash`／`IconThink`／`RunningDot`／
`ThinkingRow` ＋ `RUNNING_RING`）——那幾個正是 ⋯ 選單在用的。
（另外兩組「重複」是 PowerShell 雜湊表**不分大小寫**造成的誤判。）

> 判斷方法要可靠：第一版掃描腳本沒遮掉 template literal，給出「只有一組重複」的
> 錯誤結論。可靠的做法是直接列 `^    (function|var|const|let) NAME` 再按名字數重複
> ——4 空格縮排就是同一個作用域。

### 測試

- `test-client.mjs` **4f**：四個分區的名稱、分區列、**一次只畫一區**。
- **4g**：大廳的快速入口會跳區、**破壞性動作收在「進階」裡**（收合時不在 DOM）。
- **4h**：海報牆（有主圖用 `<img>`、沒主圖給佔位符）、點進編輯器、返回鍵回得去。
- **4i**：藏書的條目——兩種格式（uid 物件／陣列）、只改那一條、`keys` 不會被改名、
  壞掉的 JSON 回 `null` 而不是丟錯（`worldbookParse` 現在也擋頂層陣列）。
- ⚠️ 這幾條**修掉了一個空跑的斷言**：以前「設定頁要有四個分區」那條，測試從沒餵
  酒館進去，畫面只渲染「還沒有選定酒館」，而那段說明文字剛好含「對話紀錄」——
  所以它一直是綠的，卻什麼都沒驗到。`__testSeed` 現在可以逐欄餵資料。
- **4c** 從「掃原始碼字串」改成**驗行為**（檢查 id 真的傳進子元件的 props）：
  舊寫法驗的是寫法，純重構就會讓它變紅，而行為其實一樣。
- `test-agent.mjs` 的版本斷言**不再寫死**：以前是 `'tavern-agent-' + '2.5.1'`，
  而訊息卻寫著「要對得上 package.json」——它根本沒讀 manifest。這次升版就是被它
  擋下來的。現在直接讀 `package.json`，版本只有一個來源。

### 介面變動

- 版本標記升為 `tavern-2.6.0` / `tavern-client-2.6.0` / `tavern-agent-2.6.0`。
- **沒有 API、RPC 或檔案格式變動**——使用者的檔案一個字都沒改。

## 2.5.1 — 刪除對話的入口，以及 macOS 上的開發迴圈

### 三個介面修正（都照 DSH 原生的做法）

**1. 區塊標題列整列可點。** 使用者：「這好像是一個按鍵點擊摺疊，但下面的一層卻是整行
都能夠點擊摺疊。這裏風格不一致我希望遵從下面一層的做法」——酒館列本來就是「點列本體
＝展開／收合」，所以標題列照同一個規矩：**點那一列的任何地方都切換**（`cursor:pointer`），
只有右邊那顆「＋ 新增酒館」`stopPropagation`（不然按新增會順手把區塊收掉）。
摺疊按鈕留著（鍵盤與報讀器靠它），它自己切換 ＋ 擋冒泡。

**2. 聊天室貼底。** 使用者：「他應該要保持貼底，因為現在思考的時候我看不見思考要自己
手動滾下去」。我們的重繪是**指令式**的（自己叫 `render()`），所以不能在 render 裡無腦
`scrollTop = scrollHeight`——那會把正在往上翻紀錄的人硬拉回底部。規矩照原生聊天：

| 情況 | 行為 |
|---|---|
| 內容長高（思考、串流文字）＋ 使用者**貼著底** | 自動捲到底 |
| 使用者自己往上滑（離底超過 40px） | **不動他**（尊重閱讀） |
| 他再滑回底部附近 | 恢復自動 |
| 他自己**送出訊息** | 無條件貼底（剛說的話一定看得到） |

用 `scrollTop = scrollHeight`（不用 `smooth`：串流每一段都要跟得上，`smooth` 會排隊）
＋ 一次 `requestAnimationFrame` 補正（內容高度常常在這一輪之後才定下來）。

**3. 分隔線的動態感**（使用者貼了原生的 `widthHandle` 說「這裏能夠動態調整至中」）。
原生**不是畫一條固定的線**，而是：

```js
// .wSkVaW_widthHandle 的 onPointerMove —— 只改一個 CSS 變數，所以沒有重繪開銷
const box = e.currentTarget.getBoundingClientRect()
e.currentTarget.style.setProperty('--dsh-width-handle-pointer-y', `${e.clientY - box.top}px`)
```

```css
.widthHandle:after {
  background: linear-gradient(to bottom,
    transparent calc(var(--dsh-width-handle-pointer-y, 50%) - 52px),
    var(--dsw-alias-scrollbar-hover-l1) calc(var(--dsh-width-handle-pointer-y, 50%) - 12px),
    var(--dsw-alias-scrollbar-hover-l1) calc(var(--dsh-width-handle-pointer-y, 50%) + 12px),
    transparent calc(var(--dsh-width-handle-pointer-y, 50%) + 52px));
  opacity: 0; width: 3px;
}
.widthHandle:hover:after, .widthHandle[data-dragging]:after { opacity: 1 }
```

**游標在哪裡，那道光就在哪裡**（以它為中心 ±52px 淡出）。我們照抄成
`--dsh-tv-pointer-y` ＋ `onMouseMove: trackPointer`，並在拖曳中標記 `data-dragging`
（跟原生一樣持續亮著）。

### 測試（`test-client.mjs` 第 3、12 項）

- 標題列本身接得上點擊、**按鈕只擋冒泡不重複切換**、**按「＋」不會把區塊收掉**。
- 分隔線：`--dsh-tv-pointer-y` 的寫入、`onMouseMove` 有接、`:after` 的漸層以它為中心、
  `data-dragging`。
- 貼底：`stickToBottom` 的守衛（**翻紀錄時不搶**）、40px 的判定、`ref`／`onScroll` 有接、
  送出時無條件貼底。

**真瀏覽器實測**：標題列點列就展開（`aria-expanded` false → true）、
分隔線 `--dsh-tv-pointer-y` 跟著游標（`2px` → `7px`）、
對話紀錄 `distanceFromBottom: 1` 且最後一則**完全可見**、
往上滑之後 `scrollTop` 停在 `0`（新內容進來也不搶）、滑回底部恢復自動。


### 「思考」那一列（使用者：「思考的時候應該是有些圖標的，我看 dsh 也有」）

以前 `reasoning-delta` 被當成「不認識的東西」**整段丟掉**——於是模型在思考的那幾秒
畫面上**什麼都沒有**，看起來像卡住。DSH 原生在那段時間會顯示一列會流動的「思考」，
所以照它的形狀補上：

| 原生（`ReasoningRow`） | 酒館 |
|---|---|
| `IconThinkOutline14`（燈泡、14px） | 自己畫一顆同形的（原生那顆在 primitives，bundle 只拿得到 `react`） |
| 標題「思考」（`message.think`） | 一樣 |
| 可折疊、**整列可點**（`expandOnRowClick`） | 一樣（`role=button` ＋ Enter/Space） |
| 收合時顯示一段預覽（`collapsedContent`） | 一樣 |
| 串流時疊一層流動的高光（`:after` 的 `dsh-reasoning-row-sweep`） | 一樣（`@keyframes dsh-tv-sweep`，2.6s；`prefers-reduced-motion` 時關掉） |
| 放在回覆的**前面** | 一樣 |

- **預設展開、串流結束後可收合**；使用者自己點過就尊重他的選擇（`touched`）。
- **思考會寫進 `.jsonl` 的 `extra.reasoning`**——那是 SillyTavern 本來就有的自由欄位，
  所以帶著走的檔案裡思考不會丟，別的軟體也讀得懂那一則訊息。**不會混進正文**，
  沒有思考的訊息也不會長出空的 `extra`。
- 寬度上限跟氣泡一致（`max-width:78%`）：思考可能很長，鋪滿整個對話寬度會把正文擠掉。

### 「進行中」的矩陣跑馬燈：放在**側邊欄的對話列**，不是思考列

使用者：「check Room 外面的列表 check Room 列表…運作中的時候會換一個思考中的圖表的動圖」
——指的是**側邊欄包廂列表那一列的圖示欄**（`dsh-tv-slot`），不是對話頁的思考列。
第一版做到思考列上了，**已還原**。

DSH 原生的判定是 `node.session.running`（`sessionStatuses()`：有 pending interaction
優先，否則 `running` → `state: 'ongoing'`）。**我們拿不到那個欄位**——`session.list`
只回「哪個 session 綁到哪份對話」。所以改成我們自己知道的那件事：

> 對話頁**送出訊息到收到結果之間**，這一份對話就是「進行中」。

實作：模組層級的 `runningChats`（key 是 `角色/對話名`）＋ `setChatRunning()`；對話頁在
送出時標記、完成／失敗／取消時清掉，並走 `refreshChannel` 通知側邊欄。側邊欄用新的
`useRefreshChannelRerender()` 訂閱——**只重畫、不重讀**（為了一顆圖示重讀六份
`.jsonl` 是浪費）。跑完就換回原本的圖示（對話圖示或該對話的主圖插圖）。

#### 那顆動畫的精確規格（`StatusDot` 的 `ongoing`）

原型是 DSH 的 `StatusDot`（`dsh-web-frontend` 的 bundle，`state === "ongoing"`）：

```js
const RING = [[0,0],[4,0],[8,0],[8,4],[8,8],[4,8],[0,8],[0,4]]
<svg width={10} height={10} viewBox="0 0 10 10" shapeRendering="crispEdges">
  {RING.map(([x, y], i) => (
    <rect x={x} y={y} width="2" height="2"
          style={{ animationDelay: `${(i - RING.length) * 125}ms` }} />
  ))}
</svg>
```

CSS（同一份 bundle 的樣式表）：

```css
.matrix { --dsh-state-ongoing: var(--dsw-static-deepseek-450); color: var(--dsh-state-ongoing) }
.cell { fill: currentColor; opacity: .15; animation: dot-chase 1s infinite }
@keyframes dot-chase {
  0%,12.4%   { opacity: 1 }
  12.5%,24.9%{ opacity: .6 }
  25%,37.4%  { opacity: .35 }
  37.5%,to   { opacity: .15 }
}
```

**關鍵是負的 `animation-delay`**（-1000ms 起、每顆 +125ms）：同一條 1s 的動畫
依序跑過八顆方格，看起來就是一顆光點繞著方框跑。顏色是 `--dsw-static-deepseek-450`
（這台解析成 `#5686fe`）。我們把它複製成 `RunningDot` ＋ `dsh-tv-chase`，
四段透明度寫成自訂屬性（`--dsh-tv-chase-1..4`）避免數字重複兩份。

**實測驗證**（真的在瀏覽器裡量）：8 顆方格、延遲 `-1s / -0.875s / … / -0.125s`、
`fill: rgb(86, 134, 254)`、`animation: dsh-tv-chase 1s`、
當下透明度 `1.00 / .35 / .60 / .15 …`（＝真的在跑）。

> ⚠️ **寫回紀錄那一半要重啟宿主半**（`chat.messages` 是宿主半的 op）。
> 串流時的思考列不需要——那條路完全在客戶端半。

### 測試

- `test-client.mjs` 第 9 項：串流的 `reasoning-delta` 要**即時**回報、順序在正文之前、
  而且**不混進正文**（`result.text` 只有正文、`result.reasoning` 只有思考）。
- `test-client.mjs` 第 14 項（新）：思考列的**渲染**——標題「思考」／運作中是
  「思考中」、**運作中要畫出 8 顆方格與正確的負延遲**（`-1000ms` 起、每顆 +125ms）、
  **停下來就不該還有跑馬燈**、`10×10` ＋ `crispEdges`、收合時有預覽。

- 側邊欄的「進行中」：`chatIsRunning` / `setChatRunning` 的狀態機（送出→跑、
  完成→停、**別的對話不受影響**、**狀態變了要通知側邊欄重畫**），以及原始碼層級的
  契約（那一列在進行中時改用 `RunningDot`）。

  > 「點一下會展開」的**狀態轉移**沒有寫進離線測試：假的 React 不會因為 setState
  > 重繪，而我們是靠「再渲染一次」去觀察那個 `useRef` 的變化——中間只要有任何一次
  > `collect`／`flatten` 走進元件就會多跑一次、把狀態攪亂。**這一段反覆試了四次
  > 都不穩，所以停手**，改成驗可靠的部分（整列接得上點擊、預設收合、外觀），
  > 真實的點擊照 §7.4b 的規矩在瀏覽器裡走一遍。
- `test-workspace.mjs` 第 17 項（新）：思考**無損往返**——`extra.reasoning` 原樣讀回
  （含換行）、不混進正文、沒有思考的訊息不長出空的 `extra`、壞行與缺欄位照舊可讀。


### ⚠️ 修掉一個「CSS 同名 class」造成的排版災難（使用者：「打直顯示，一行根本顯示不全」）

使用者回報側邊欄的對話列變成**直的**、一行顯示不全。查下去是三個坑疊在一起：

**坑 1：同一個 class 名被定義兩次，而 `flex-direction` 只寫在其中一條。**
`.dsh-tv-chat` 既是 v1 的「對話頁訊息容器」（`display:flex; flex-direction:column;
gap:10px`）又是新加的「側邊欄對話列」（`display:flex; align-items:center; gap:0`）。
兩條同分特異度、後者勝出——**但 `flex-direction:column` 只在前者裡，不在後者的
`display:flex` shorthand 裡，所以它留了下來**，對話列就被壓成直的：圖示、標題、
時間疊在一起，`elementFromPoint` 在標題中心打到的是「時間」。
現在拆成 `.dsh-tv-chatBody`（容器）與 `.dsh-tv-chatRow`（列），並且
**列自己明寫 `flex-direction:row`**。

> 順手抓到同型的另外兩組：`.dsh-tv-split`（設定頁兩欄 grid ↔ 側邊欄拖曳分隔線）
> 與 `.dsh-tv-tag`（檔案標籤 ↔ 舊版殘留標記）。四組全部拆開。
> `test-client.mjs` 第 12 項現在**掃出「同一個 dsh-tv class 被定義兩次」**——
> 同名 CSS 規則幾乎一定是 bug。

**坑 2：portal 到 `document.body` 的選單拿不到主題變數。**
選單本來寫 `background: var(--dsw-alias-bg-elevated, #161a22)`——而
**`--dsw-alias-bg-elevated` 在 DSH 的主題裡根本不存在**，於是它落到深色 fallback，
淺色主題下變成「深色字壓深色底」，**三個標籤全部看不見**（只有圖示看得到）。
現在 `inheritTheme()` 從側邊欄當下的 computed style 取真正的底色與文字色，
直接寫在**選單框**上（寫在容器上不夠——容器的底色不一定透上來）。

**坑 3：`event.currentTarget` 在繪製選單時已經是 `null`。**
React 的合成事件會在處理器跑完之後清掉它，所以「從按鈕往上找側邊欄底色」那條路
根本沒跑。現在另外用 `document.querySelector('.dsh-tv-region, …')` 當錨點。

**同時照原生補齊的寬度約束**（原生 `.title` 那一條的精髓是 `min-width:0`）：
`.dsh-tv-title` 逐字照原生（`text-overflow:ellipsis; white-space:nowrap;
min-width:0; overflow:hidden`，**不寫 `width`**——實測寫 `width:0` 會讓它變成 0 寬），
`.dsh-tv-group` / `.dsh-tv-streetInner` / `.dsh-tv-projectText` 補上 `min-width:0`。

**驗證方式（真的在瀏覽器裡量的）**：列 260×32、`flex-direction:row`；
四個部分 `(20,16) (40,178) (224,40)` 同一條水平線、不重疊；
hover 時時間 `display:none`、⋯ `display:flex`（16px）；
選單 140×94、三項齊全、`insideViewport: true`、底色 `rgb(242,245,250)`、文字 `rgb(29,37,57)`；
改名行內輸入 202×20、有 focus、Enter 後收掉。


### 對話列照原生會話列（使用者：「check Room 應該和現在 DSH 靠近甚至一樣」）

使用者貼了兩段 HTML 來比對，問題一眼就看得出來：原生的會話列是
`slot → title → time → rowActions`，**酒館的對話列沒有最後那一塊**
——CSS 早就為 ⋯ 寫好了（`hover 時時間讓位`、`hover 時動作出現`），
但**沒有任何程式碼渲染那個 span**，所以那個原生行為永遠不會發生。

現在四個部分齊全，而且行為照原生：

- **hover 時時間讓位給 ⋯**、選單開著時也一樣（純 CSS，跟原生同一組選擇器）。
- **⋯ 開「改名／分支／刪除」**（原生是「改名／分支／封存」；封存沒有對應的酒館概念）。
- **改名是行內輸入框**，不是彈窗：Enter 送出、Esc／失焦取消，樣式照原生
  `.renameInput`（14px/20px、4px 圓角）。
- **選單 portal 到 `document.body`**，所以不會被側邊欄的 overflow 裁掉
  ——原生也是 `createPortal(…, document.body)`。客戶端 bundle 的 `require`
  只拿得到 `react`（沒有 react-dom），所以自己建容器 + 固定座標。
- 點外面或 Esc 關閉；選單開著時那一列維持 `menuOpen` 的底色（原生一樣）。
- `gap:12px`（原生 `.rowActions{gap:12px}`）、標題邊距 `0 6px 0 4px`（原生一樣）。

### 新 op：`chat.rename`

改一份對話的名字＝改那個 `.jsonl` 的檔名，但一份對話的**身分**四處都記著同一個名字，
所以四處一起搬：

| 哪裡 | 怎麼搬 |
|---|---|
| `chats/<角色>/<名>.jsonl` | 內容原樣搬到新檔名（**獨佔建立，撞名自動編號、不覆蓋**） |
| `.sessions/<id>.json` | 綁定裡記的對話名一起改（不然那份對話會失去它的 session） |
| `art/chats/<角色>/<正規化名>/` | 插圖資料夾跟著改名（目標已存在時不合併、不覆蓋） |
| `tavern.json` 的 `assets` | 主圖 key `chat:<角色>/<名>` 一起改 |

回傳的 `name` 才是**真正用的名字**（撞名時會換編號），呼叫端一定要用它。
名字沒變時當成功（呼叫端常常是「按了 Enter 但沒改」）。

### 分支：接 DSH 自己的 `remote.session.fork`

原生會話列那一顆「分支」就是 `session.fork({sessionId, atSeq?})`。酒館把它接上檔案：
`fork` → `chat.create` 開新檔 → `chat.append` 把目前看到的訊息寫過去 → `session.bind`。
兩個真實限制都**在選單上直說**，不給「按了沒反應」的按鈕：
還沒有任何綁定（這份對話還沒真的聊過）→ 明說要先開啟並說一句話；
這台 DSH 的 remote 沒有 `fork` → 那一項變灰並說明原因。

### 順手修掉的脆弱點（同型 bug 的預防）

使用者這輪讓「元件被直接呼叫」的路徑變多，於是幾個「假設宿主半一定回陣列／物件」
的地方在渲染期丟錯——那會讓整個 `main` 面板變成死格（`plan.md` §7.4b 那個型別）。
全部改成防禦式：`MapOverview` / `MapCharacters` 的 `characters`、
`summary.counts`、`MapTavernActions` 的名稱。**它們是防禦，不是這輪的功能。**

### 測試

- `test-workspace.mjs` 第 16 項（新）：改名——檔案／綁定／插圖資料夾／主圖 key
  四處一起搬、**撞名不覆蓋**（回傳的是真的名字）、不存在的對話與空名字要報錯。
- `test-client.mjs` 第 12 項（新）：對話列的 `slot／title／time／actions` 四個部分
  齊全、⋯ 有 `aria-haspopup`、按下去畫出 portal 到 body 的選單、三項名稱與順序。
- `test-client.mjs` 第 13a／13b／13c（新）：改名（按鈕只開行內輸入，Enter 才送 op，
  四個參數都對）、刪除（三個參數）、分支（`fork` → 新檔 → 寫訊息 → 綁定新 session）。


### 為什麼

兩個都是「東西已經做好了、但使用者碰不到」的問題。

1. **`chat.delete` 早就做好了**（宿主半有這個 op、`smoke.mjs` 第 14 項也驗過行為），
   但**畫面上沒有入口**——開了一份對話就永遠刪不掉，只能去檔案總管手動刪檔。
   `docs/plan.md` §7.6 因此把它列為「UI 階段的第一件事」。
2. **改了程式碼卻不會生效**：profile 是從 GitHub 的 tarball 裝的
   （`refs/heads/main.tar.gz`），所以工作區的檔案跟 DSH 實際載入的
   `node_modules/dsh-tavern` 是兩份**實體複製**，改完還要手動複製過去，
   而且 tarball 會被 pnpm 的快取騙到（裝到的還是舊版）。

### 刪除對話（新）

設定頁的「💬 對話紀錄」分區，每一列多一顆**刪除**（danger 樣式）：

- **按一下不會刪**，只會在那**一列底下就地展開確認**（「確定要刪掉
  `角色/對話.jsonl` 嗎？檔案會真的從磁碟上消失，救不回來。」＋「確定刪除」／「取消」）。
  一次只確認一份——按另一列的刪除會把上一列的確認收掉。
- 不用 `window.confirm`：這個 repo 從來沒有用過對話框，而且側邊欄在最底部、
  彈窗會被裁掉（⋯ 與 ＋ 都是為此改成不彈窗）。就地展開的確認列在離線測試裡也看得到。
- 刪除成功後：清單重讀、`summary.counts` 重算（通知兩條通道）、訊息寫出
  「已刪除 `角色/對話.jsonl`」——**並說明對話室插圖留著、session 綁定一起解掉**
  （跟刪角色同一個規矩：只刪那一份，不連帶清別的）。
- **正在看的那份對話被刪掉時**，對話頁會自己發現（訂閱「內容變了」的通知 → 重讀
  `chat.list` → 找不到就退回這間酒館的設定頁並說明原因），而不是留在一頁已經不存在的
  紀錄上、等使用者送出訊息才得到「找不到這份對話」。

### 開發迴圈：改用 `link:`（兩台都改了）

```sh
dsh plugin --profile web add "link:/path/to/dsh-tavern"
```

於是 `~/.dsh/profiles/web/node_modules/dsh-tavern` 是一條 symlink，指回工作區。
改 `lib/client.js` 之後**重新載入頁面就生效**（DSH 內建的 `dsh-client-hmr` 每 500ms
對 client bundle 重新取 hash，內容變了就透過 `/plugins/events` 推 `rebuilt`）；
動到宿主半（`lib/index.js`）仍然要重啟 `dsh web`。

> 驗證方式（不必問人）：抓 `/plugins/??dsh-tavern/client.js&rev=<rev>`，下載到的 bundle
> 應該含著你剛改的字串；`/plugins/events` 會列出每個 bundle 的 `rev`。

**另一台（2026-09-18 補上）**：它原本裝的是 `…/archive/refs/heads/main.tar.gz`，
也就是**實體複製**——改了工作區 DSH 完全不知道，而且那個 tarball 寫法會被 pnpm 快取
騙到舊版。同一條 `dsh plugin --profile web add "link:<repo>"` 就修好了。

三個實測的細節值得記下來（下次換機器會再遇到）：

- `dsh plugin` 是 **pnpm 的 passthrough**，而 profile 在沙箱外，所以要升級權限才跑得動。
  症狀是 pnpm 回 `unable to open database file`（它打不開自己的 store）——**不是 pnpm 壞掉**。
- 它印 `Packages: +4 -13` **不代表有依賴被動到**：比對 lockfile 的套件集合是 219 → 219，
  完全不變；那只是 pnpm 在 `nodeLinker: hoisted` 下把套件在 `node_modules` 根目錄與
  store 之間重擺。真正該檢查的是**其他插件的版本有沒有漂移**（這一台：零漂移）。
- 轉換**不必重啟**就已生效一半：DSH 會自己重新掃描，`/plugins/events` 的 rev 會變。

轉換後直接驗過：抓下來的 bundle **逐位元組等於工作區的 `lib/client.js`**
（只多一個 `sourceMappingURL` 包裝）；在檔案尾端加一行標記 → rev 自己從
`161ea301dddc` 變成 `1e28d3a90cb4`，bundle 就含那行——**沒有重啟**。

⚠️ 代價要知道：`link:` 之後**工作區的 `lib/client.js` 壞掉＝當場的 GUI 白屏**
（客戶端插件掛不起來就是整個 GUI 白屏）。所以改完**先跑 `npm test` 再重新載入頁面**。

### 文件更正

- `README.md` 說「五支測試」、版本標記寫 `2.4.0`——實際是**八支**、`2.5.0`
  （現在是 `2.5.1`）。
- `docs/plan.md` 的環境那節整節是 Windows 路徑與「沒有 git」的前提，
  在 macOS 這台上已經不成立。

### 介面變動

- 版本標記升為 `tavern-2.5.1` / `tavern-client-2.5.1` / `tavern-agent-2.5.1`；
  `package.json` 與 `dsh.plugin.json` 為 `2.5.1`。
- 沒有 API、RPC 或檔案格式變動：`chat.delete` 本來就在，只是多了入口。

### 測試

- `test-client.mjs` 第 11 項（新）：確認列、`chat.delete` 的三個參數
  （`id`／`character`／`chat`——這一頁踩過兩次「少了酒館 id 就被當成角色 id」的坑）、
  **列上的按鈕不可以直接刪**、沒有 `window.confirm`。
- `test-client.mjs` 第 4d／6b 項：`MapChatFiles` 的通知點從 1 個變 2 個（建立 ＋ 刪除）、
  `useRefreshVersion` 的訂閱從 4 處變 5 處（對話頁也訂閱了）。

## 2.4.0 — 新建酒館附老闆娘與世界書；補齊設定頁的入口

### 為什麼

使用者：「**預設就是，新建酒館的時候會給一個酒館老闆娘和世界書**」。
一間剛開好的酒館原本是空的（0 人物卡、0 世界書），面對一片空白很難下手。

使用者同時指出：「**設定的 gui 麻煩你重新看重新找，好像和你想的不一樣**」——
確實不一樣。我原本以為的設定頁比實際的完整，實際走一遍之後發現三個缺口。

### 新建酒館的預設內容（新）

按 ＋ 選資料夾之後，除了四個目錄與設定檔，還會附上：

```
characters/老闆娘.json    一位可以直接開始的老闆娘
worldbooks/酒館.json      一本寫著「這間店／老闆娘／店規」的世界書
```

三個刻意的約束：

- **只在新建立時給。** 不放在任何讀取路徑上——讀取寫檔曾經讓「舊版殘留」的
  標記自己消失，那個 bug 花了兩輪才修完。**既有的酒館不會被補**。
- **只補不覆蓋。** 目標檔案已存在就跳過；使用者自己改過的老闆娘不會被蓋掉。
- **寫出來的是一般的卡與世界書**，沒有任何隱藏格式，要改要刪都很容易。

內容寫在 `lib/defaults.js`。世界書用**原生形狀**（`entries` 是以字串化 uid
為 key 的物件），跟使用者從別處匯入的世界書是同一種東西、同一套編輯器。

### 設定頁（側邊欄 ⋯ 進去的那頁）補齊

實際走過一遍之後發現的問題：

- **「💬 對話紀錄」分區完全沒有動作可做**——只能從側邊欄那顆 hover 才出現的 ＋
  開新對話，設定頁這邊找不到入口。現在有「**＋ 新對話**」，會就地列出角色讓你挑
  （不彈窗：酒館街在最底部，彈窗會被裁掉，跟 ⋯ 同一個理由）。
- **世界書只能新增空白的**。現在有「**📥 匯入世界書**」，跟匯入卡片一樣走二進位
  body，**不做任何欄位轉換**（原始 JSON 原樣寫入）。接受 `{entries:…}` 與陣列兩種
  方言；不像世界書的檔案會早點拒絕並說明原因。
- 世界書的 id 由**檔名**推導，不是 `data.name`——後者是 SillyTavern **內嵌**方言
  才有的欄位，原生世界書沒有，照它走會得到 `worldbook-<時間>` 這種爛名字。

### 介面變動

- 新增 `lib/defaults.js`（`package.json` 的 `files` 要帶上）。
- 新增 RPC `worldbook.import`（二進位 body）。
- `tavern.add` 的 `skeleton` 現在會回報預設內容的兩個檔案
  （`characters/老闆娘.json`、`worldbooks/酒館.json`）。
- 版本標記升為 `tavern-2.4.0` / `tavern-client-2.4.0`。

### 測試

- `test-workspace.mjs` 第 12 項：seed 產生正確的卡與世界書、欄位不可是空的、
  第二次 seed 不建立任何東西、**不覆蓋使用者改過的版本**。
- `smoke.mjs` 第 4 項加上預設內容的斷言、新增第 5b 項（世界書匯入：原樣寫入、
  兩種方言、不像世界書就拒絕）。
- `test-registry.mjs` 第 4 項改成**相對**比對（新建附老闆娘之後基準不是 0）。

## 2.3.0 — 儲存層強化：原子寫入、來源圍籬、PNG 卡匯入

三份參考專案（SillyTavern、flizzywine/dsh-tavern、dsh-portable-tavern）研究完之後，
依 [`docs/storage-layout.md`](docs/storage-layout.md) 的定稿實作。判斷過程見
[`docs/design-comparison.md`](docs/design-comparison.md)。

### 原子寫入（最重要的資料安全修正）

以前每個寫入都是裸的 `writeFile`，而它在磁碟上是「**截斷 → 寫入 → 完成**」三步。
如果行程在截斷與完成之間死掉（斷電、被強制關閉、crash、磁碟滿），
檔案就停在「被截斷、只寫了一半」——**舊的沒了、新的也不完整**。
`tavern.json` 裝著主圖設定與酒館名稱，壞掉就是這些設定全丟。

現在全部走 `lib/write.js` 的 `atomicWrite`（寫暫存檔 → `rename`），
所以斷電時只有兩種結果：**舊的完整版本**，或**新的完整版本**。

`smoke.mjs` 第 13 項**真的在寫入途中把行程 SIGKILL**，然後檢查檔案：
新寫法留下原本的完整內容，而對照組（直接寫檔）被截斷成 0 bytes。
（寫這個測試本身踩了兩次坑：第一次「等 250ms 再殺」根本殺在寫完之後；
改用「看到暫存檔出現才殺」才真的命中寫入中。）

涵蓋範圍：`~/.dsh/taverns.json`、`tavern.json`、`characters/*.json`、
`worldbooks/*.json`、`art/**`。

### 不再覆蓋：獨佔建立與碰撞重試

- `ensure()` 建 `tavern.json`／`README.txt` 改用 `open(..., 'wx')`，
  取代「先 stat 再寫」——後者在檢查與寫入之間有空窗。
- `createChat` 改用 `createUnique`（撞名換編號重試）。
  舊寫法有兩個毛病：檢查與寫入之間有空窗，而且最後一次嘗試撞名時
  會把裸的 `EEXIST` 丟給使用者。

### 來源圍籬（安全）

研究時查出：**DSH 的 `webserver` 沒有任何來源檢查**（`origin`／`sec-fetch`／
`remoteAddress` 全部零命中），而 SillyTavern 自己有 CSRF token
——所以「本機的其他網頁可以打這個 port」是真實的威脅模型。
我們的 op 會**建立檔案**（`tavern.add`，路徑由呼叫端決定）、**寫入**（`assets.write`）、
**刪除**（`assets.delete`），所以兩條路由都套了四項檢查：

1. 連線來源必須是迴圈位址
2. `Host` 也必須是迴圈位址（擋 DNS rebinding）
3. `Sec-Fetch-Site` 不可以是 `cross-site`
4. 有 `Origin` 時，它的 host 必須與 `Host` 相符

刻意留了遠端出口：`DSH_TAVERN_ALLOW_REMOTE=1` 放行非本機來源，
但第 3、4 項仍然有效——放寬的是「只有本機」，不是「關掉防護」。
`smoke.mjs` 第 14 項對每一種繞過手法各測一條。

### PNG 卡匯入（新）

真實世界的酒館卡大多是一個 `.png`，資料藏在 tEXt chunk 裡。新增 `lib/pngcard.js`：

- 讀 `chara`（V2）與 `ccv3`（V3），**`ccv3` 優先**、關鍵字**大小寫不敏感**、
  解碼鏈是 base64 → UTF-8 → JSON（與 SillyTavern 一致）。
- **只做讀，不做寫。** 兩個參考專案也都只做單向——PNG 內嵌是「讀別人的卡」的需求。
- ⚠️ **不相信任何長度欄位**：PNG 每個 chunk 開頭是 4 bytes 的長度宣告，
  一個惡意檔可以宣告 4GB。每一步都先確認剩餘位元組夠不夠，
  超過上限就拒絕。`smoke.mjs` 第 16 項用一個宣告 `0xFFFFFFFF` 的 chunk 驗證它
  「瞬間拒絕而不是真的去讀」。
- 匯入時：資料寫成 `characters/<id>.json`、**原始 PNG 位元組**留在
  `originals/cards/<id>.png`（tEXt 以外的位元組我們重建不出來）、
  那張 PNG 同時成為這張卡的插圖。
- 原版**同名不覆蓋**：原版的意義就是「第一次匯進來的樣子」。
- 瀏覽器半：人物卡分區多了「📥 匯入卡片」按鈕（接受 `.png` 與 `.json`）。

### 把「對不起來」變成不可能

匯入與上傳都是二進位 body，op 名稱寫在程式碼裡的**字面字串**上。
`smoke.mjs` 的跨半契約檢查（第 12 項）因此也擴充成同時掃 `rpc('…')` 與
`sendFile('…')`，並且**容忍跨行呼叫**——第一版就是因為 `sendFileExpectOk(\n 'character.import'`
跨行而掃不到，還加了「必須掃到這兩個二進位 op」的自我檢查。
現在 25 個 op 全部被涵蓋。

### 無損往返的保證（取代 `.original` 備份）

討論時決定**不做**「外部改過偵測」，也不為每個檔案預留 `.original` 備份
（使用者確認「同時用編輯器開著同一個檔」不是真實情境）。改為用
**規則 + 測試**保證：`test-workspace.mjs` 第 10、11 項拿真實形狀的卡與世界書
（含未知欄位、`extensions.regex_scripts`、`character_book`、世界書的兩種方言）
寫入再讀出，**逐欄位必須相等**。

### 介面變動

- 新增 `lib/write.js`、`lib/pngcard.js`（`package.json` 的 `files` 要帶上）。
- 新增 RPC `character.import`（二進位 body）。
- 版本標記升為 `tavern-2.3.0` / `tavern-client-2.3.0`。
- 新增環境變數 `DSH_TAVERN_ALLOW_REMOTE`。
- 新增 `originals/cards/` 目錄（只有匯入 PNG 卡時才會出現）。

## 2.2.0 — 插圖變成「一組圖 + 主圖」，並修掉兩個實際踩到的 bug

### 為什麼

2.1.0 的 `art/` 只是 `mkdir` 出來的一個空目錄：宿主半有一條 `/api/dsh-tavern/files`
在服務它，但**瀏覽器半從來沒有呼叫過**，也沒有任何上傳端點。也就是說插圖一直是死路，
而且結構是孤島——人物卡是一個 `.json`，圖卻丟在一個對不上任何東西的 `art/<角色>/`。

同時使用者回報了兩件事：

1. **「按 ＋ 新增酒館，連預設酒館也一起出現」**——真相是那筆紀錄從 2026-09-16（v1）
   就躺在 `~/.dsh/taverns.json` 裡，而酒館街預設收合，所以按 ＋（新增後清單會自動展開）
   才第一次看到它。不是新增邏輯多建了一間，是舊資料沒有清理路徑。
2. **「＋ 新增角色」按了沒反應**——面板呼叫 `character.create`，宿主半沒有這個 op，
   永遠回 `unknown op`。而所有測試都是綠的，因為兩半各自被測、沒有人測它們之間的介面。

### 插圖（新）

- **模型改成「一個東西 → 一組圖 → 指定主圖」**。一個角色的微笑／生氣／動作差分
  可以幾十張，所以不是「一張立繪」。
- 掛載點（`art/` 底下，一項一個資料夾）：
  `art/characters/<卡 id>/`、`art/worldbooks/<書 id>/`、
  `art/chats/<角色>/<對話名>/`、`art/tavern/`（店面／背景圖）。
- **資料夾就是清單**：直接丟圖進去就生效，不用改設定檔。
  主圖記在 `tavern.json` 的 `assets`（`{"character:老闆娘": "微笑.png"}`）；
  沒指定＝檔名排序第一張，指定的被刪掉會自動退回第一張。
- **上傳走二進位**（`assets.write`：body 是圖片本身，種類／擁有者／檔名放 query），
  不是 base64 JSON——一張圖好幾 MB，base64 會再多 33%。
- **只信內容不信副檔名**：檢查 magic bytes（png/jpg/gif/webp/avif/bmp），
  宣告 jpg 但內容是 png 就存成 `.png`。單張上限 8MB、單一實體上限 400 張、
  同名自動編號（`微笑.png` → `微笑-2.png`，不覆蓋）。
- 讀圖路由 `GET /api/dsh-tavern/assets/...`：逐段驗證路徑（擋跳脫）、
  正確 `content-type`、`nosniff`、etag（304）。
- 面板：人物卡清單顯示主圖小臉、卡片編輯器／世界書／對話頁／酒館設定頁各有一個
  插圖管理器（縮圖格、點縮圖換主圖、✕ 刪除、整批拖放、可同時選多檔）。

### 修掉的 bug

- **`character.create` 不存在** → 宿主半補上這個 op，「＋ 新增角色」現在真的會建卡。
- **`writeSettings` 只認得 `name`/`note`** → 面板設的主圖會被**默默丟掉**
  （看起來像「設了沒生效」）。現在 `assets` 有自己的分支並逐項驗證。
- **刪掉主圖時的邏輯錯誤** → 以前會拿 `null` 去指定主圖而丟錯；
  現在改成「刪掉的正好是主圖時，才把主圖換成還存在的那一張」。
- **上傳後「第一張自動當主圖」永遠不成立** → 判斷順序寫反了（先寫檔才看「本來有沒有圖」）。
- **`art` 計數把資料夾也算進去**（`readdir().length`）→ 改成遞迴數圖片檔。
- **舊版殘留的酒館** → `summary()` 新增 `scaffolded`（有沒有 `tavern.json`），
  面板把它標成「舊版殘留」並在設定頁說明來龍去脈；移除仍然只動清單、不刪檔案。

### 介面變動

- 新增 `lib/assets.js`；`package.json` 的 `files` 要帶上它。
- 新增 RPC：`character.create`、`assets.list`、`assets.write`（二進位）、
  `assets.primary`、`assets.delete`。
- **移除** `GET /api/dsh-tavern/files`（從來沒有被用過，而且它的路徑檢查比新的鬆）。
- `character.list` / `worldbook.list` / `chat.list` 的回應多了 `assets`
  （`{ items, primary, owner }`）；`chat.list` 的每一筆多了 `assetId`。
- 對話名可能含不能當資料夾名的字元（`&`、空白、emoji），所以插圖資料夾用的是
  正規化後的 `assetId`（`assets.js` 的 `assetOwner`），主圖設定則用原始名字當 key。
- 版本標記升為 `tavern-2.2.0` / `tavern-client-2.2.0`；`package.json` 與
  `dsh.plugin.json` 為 `2.2.0`。
- **沒有舊插圖要相容**：`art/` 本來就是空的（沒有任何程式寫過它），
  所以不做舊路徑遷移。舊的 `art/<角色>/*` 檔案仍然留在磁碟上，只是不再被讀取。

### 測試

- `smoke.mjs`：新增第 7～10 項（插圖上傳／自動編號／主圖／讀取／刪除、
  非圖片與路徑跳脫與超大檔的防護、對話室與店面、移除不刪插圖）、
  第 11 項（舊版殘留辨識）、以及第 12 項
  **跨半 op 契約**——把瀏覽器半原始碼裡的每個 `rpc('...')` 掃出來，逐一確認宿主半
  真的有那個路由。這一條就是為了讓「＋ 新增角色」那類 bug 不可能再靜默通過。
- `test-workspace.mjs`：新增第 8～9 項（assetId 正規化與撞名、magic bytes、
  路徑防護、主圖寫得進 `tavern.json` 且清得掉）。
- `test-client.mjs`：新增第 7 項（插圖 UI 的標題／加入按鈕／二進位上傳／拖放區，
  並確認不再引用退役的 `/files` 路由）。

## 2.1.0 — 啟動時什麼都不做，按了才做

### 為什麼

2.0.0 雖然把核心服務都拿掉了，但側邊欄的「酒館街」一掛載就會自己去讀 `tavern.list`。
側邊欄區塊是一直掛在畫面上的，所以那等於「DSH 一開啟這個插件就在做事」——出問題時
**看不出是哪一個動作卡住**。使用者要的是：把動作交回自己手上，按了才動，
這樣壞掉時一眼知道是哪一顆按鈕壞了。

### 改了什麼

- **酒館街預設收合**：沒展開時只畫標題列（啤酒杯 + 「酒館街」+ ＋），
  **一個請求都不送**。按標題列才展開並第一次讀 `tavern.list`。
- 展開後讀取失敗會顯示 `讀取失敗：…`，不會假裝在讀取。
- 酒館列、⋯（設定）、＋（新對話）、對話列、世界書／人物卡／對話紀錄，
  全部維持「按下去才讀」。
- `test-client.mjs` 新增第 2b 項：用 fetch 監聽釘死
  **`apply()` 零請求 → 掛載零請求 → 按「顯示酒館街」剛好一個 `tavern.list`**。

### 介面變動

- 版本標記升為 `tavern-2.1.0` / `tavern-client-2.1.0`；`package.json` 與
  `dsh.plugin.json` 為 `2.1.0`。
- 沒有任何 API 或檔案格式變動：酒館資料夾結構、`tavern.json`、註冊表格式都不變，
  升級後舊資料照樣讀得到。

## 2.0.0 — 砍掉重練：只留 UI 與檔案

### 為什麼

v1 在 macOS 上安裝後 **DSH 完全卡死、開不起來**。v1 的宿主半在啟動時會跟一堆核心服務
打交道（`settings` 命名空間＋自製 schema、`agents` 接管、`agent/request` 覆寫、
`systemPrompt` 注入），彼此糾纏，任何一個卡住就等於整個 `dsh web` 起不來。

所以 v2 的目標很明確：**只留下不會出事的東西**。

### 拿掉的（整個刪除，不是關掉）

- `settings` 命名空間、自製 schema（`lib/schema.js`）與所有參數持久化
- `agent/request` waterfall 覆寫、`systemPrompt` 段落注入、`agents` 接管
- 顯示層正則引擎（`lib/regex.js`）與卡片預覽
- 舊資料遷移、自動認養 `~/.dsh/tavern/`、「預設酒館」、自動建立任何酒館
- 階段 2–5 的佔位面板（對話循環、世界書觸發、立繪、MCP）

宿主半現在**只依賴 `webServer` 一個服務**，其餘全是檔案 I/O。

### 留下的

- 側邊欄的「酒館街」區塊（度量與互動照抄原生工作區瀏覽器：34px 列、hover 才出現的
  ⋯／＋、展開其餘 N 個、可拖曳的分隔線、彩色燈籠與啤酒杯圖示、每間酒館可自訂圖示）
- 中間的設定頁：🏠 這間酒館／🎭 人物卡／📖 世界書／💬 對話紀錄
- **新增酒館＝在選定的資料夾裡建立結構**：
  `characters/`、`worldbooks/`、`chats/`、`art/`、`tavern.json`、`README.txt`
- 人物卡：SillyTavern 信封、未知欄位原樣保留、裸卡與壞卡都處理
- 世界書：原始 JSON 讀寫
- 對話：列出 `chats/<角色>/*.jsonl`，＋ 會寫出帶 SillyTavern 標頭的檔案
- 設定卡片：退避重試、失敗顯示錯誤（不再卡在「工作區讀取中…」）

### 介面變動

- 版本標記：`tavern-2.0.0` / `tavern-client-2.0.0`
- 測試從七支減為五支（`test-schema.mjs`、`test-regex.mjs` 隨功能移除）
- `docs/architecture-seams.md` 刪除（它描述的是已移除的階段 2–5 接縫）

## 1.0.0 — 第一版正式釋出

酒館模式變成獨立的 DSH 插件：側邊欄酒館街、人物卡／世界書／對話的檔案管理、
生成參數與提示詞注入（於每次模型請求生效）、顯示層正則引擎。

（該版本的生成參數／提示詞／agent 相關功能已在 2.0.0 移除。）
