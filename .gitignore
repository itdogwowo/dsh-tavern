# 不需要進版控的東西
node_modules/
pnpm-lock.yaml
*.log
npm-debug.log*

# 編輯器／系統
.vscode/*
!.vscode/settings.json
.idea/
.DS_Store
Thumbs.db
desktop.ini

# 測試產物
*.tmp
coverage/
# 截圖與隨手放的圖不進版控。
*.png
# …但出貨範例的角色卡要進版控——它本來就是一張 PNG 卡（圖與資料同一個檔）。
# 這是唯一開放的 PNG，範圍窄到只有那個資料夾。
#
# ⚠️ **這個檔案必須是 LF。** 它原本是 CRLF，於是這一行的結尾帶著 \r，
# 而 git 會把 \r 當成規則的一部分——`!samples/characters/*.png\r` 永遠比對不到
# 任何東西，例外等於不存在，範例卡被上面的 `*.png` 吃掉
# （症狀：`git status` 只看得到別的變更，那張 PNG 完全不見）。
!samples/characters/*.png

# 個資：本機專用，絕對不進版控
# 黑名單本身就是機密——把公司名寫進公開檔案來防外洩是自相矛盾的。
# 見 docs/privacy-runbook.md 第 2.2 節。
.privacy-denylist.txt
AGENTS.local.md
CLAUDE.local.md
